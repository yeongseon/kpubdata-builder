openapi: 3.1.0
info:
  title: KPubData Builder Service API
  version: "1.22.0"
  description: >-
    Builder 중심 서비스 계약. BuildSpec 검증, preview, build 실행, artifact 조회,
    계약 버전 조회를 제공한다. CLI와 HTTP service mode(#36)가 동일한 도메인 계약을
    공유한다. 본 문서는 BuilderService(service/app.py)의 dispatch 라우팅과 실제
    wire 형태를 기계 판독용(OpenAPI 3.1) 단일 소스로 고정하며, Studio 같은 소비자가
    타입 클라이언트를 생성하는 기준이 된다. v0.4에서는 동기식 build만 유지하며
    비동기/publish 엔드포인트는 제외된다(ADR 0002, #226). v1.5.0은 built dataset
    단위 조회(dataset_id로 여러 run을 grouping)와 Bronze/Silver/Gold stage
    summary/preview API를 추가했다(#488, additive). v1.6.0은 구조화된 Quality/Schema
    Drift 결과(PASS 포함 모든 evaluated check)와 dataset별 quality history/trend
    API를 추가한다(#486, additive — 기존 엔드포인트는 변경되지 않는다). `GET
    /builds/{run_id}/quality`는 `availability`(`available`/`partial`/`unavailable`)와
    `evaluated_checks`를 함께 반환해 "0건 평가"와 "애초에 계산된 적 없음"을
    구분한다(#514, additive). v1.7.0은 Silver/Gold read-only `/query`를
    추가한다(#504, additive). v1.8.0은 stable principal별 encrypted Provider
    credential CRUD와 Provider status/test API를 추가한다(#492, additive). v1.9.0은
    `/query` 응답에 child startup과 Polars engine 실행 시간을 추가한다(#523, additive).
    v1.10.0은 `POST /preview`에 Source↔Silver diff와 sample_mode(first/random)
    옵션을 추가한다(#497, additive — 기존 필드는 유지된다). `limit`의 상한(1000)을
    새로 도입해 그 이상 값을 요청하면 400을 반환한다(behavioral tightening;
    이전에는 상한이 없었다). `limit`이 행 수만 제한하므로 wide dataset에서
    `diffs` 자체가 무제한 커질 수 있어, `diffs`도 별도 상한(1000)으로 자르고
    `diff_truncated`로 명시한다 — `transform_summary`의 집계는 잘리지 않는다.
    v1.11.0은 `GET /monitoring/summary`와 `GET /monitoring/builds`를
    추가한다(#516, additive — 기존 엔드포인트는 변경되지 않는다). Studio
    Monitoring 화면을 위한 시스템/집계 observability만 다루며 Run 단위 이벤트는
    별도(#496)다. Async build queue/worker 상태는 기존
    `AsyncBuildExecutor`/`AsyncBuildJobRegistry`(#511/#513)의 read-only
    snapshot을 반영한다 — 정상 Builder runtime에서는 `availability: available`
    이며 실제 0건과 `unavailable`을 구분한다. Builder API latency는
    healthy/degraded 임계값 근거가 없어 판정 없이 raw
    `sample_count`/`p95_latency_ms`만 제공한다. 대신
    `MonitoringSummaryResponse.status`(`healthy`/`degraded`)는 required
    subsystem(`api`/`queue`/`workers`/`artifact_store`)의 availability로부터
    계산되는 deterministic aggregate이며 latency threshold는 사용하지 않는다.
    Provider 상태는 요청마다 네트워크 프로브를 유발하므로(#492) 이번
    버전에서는 응답에도, aggregate status 판정에도 포함하지 않는다.
    v1.12.0은 BuildSpec.composition(JoinSpec)과 POST /build 응답의 composition
    키, manifest.composition(CompositionProvenance)을 추가한다(#506, additive
    — 기존 필드/엔드포인트는 변경되지 않는다). v1.13.0은 Public API·File·URL
    source를 동일한 canonical contract와
    Bronze→Silver→Gold pipeline으로 처리한다(#498, additive). BuildSpec
    `sources[]`에 `kind`(`public_api`/`file`/`url`)를 추가하며, `kind`를 생략한
    기존 source는 항상 `public_api`로 해석되어 기존 동작이 그대로 유지된다.
    `kind="file"` source를 위해 `POST /uploads`, `GET /uploads/{upload_id}`,
    `DELETE /uploads/{upload_id}`를 추가한다 — 업로드는 principal의 owner_id로
    격리되고 서버가 즉시 파싱 가능성을 검증한다. `kind="url"` source는 P0
    범위(GET, Auth=None, https만 허용)로 SSRF를 방어하는 safe fetch(DNS
    재검증, redirect마다 재검증, 응답 크기 상한)를 거친다. 두 kind 모두 기존
    Bronze artifact/provenance 계약을 그대로 재사용하며, provenance/manifest에
    로컬 파일 경로나 쿼리스트링 secret을 남기지 않는다.
    v1.14.0은 `GET /builds/{run_id}/events`를 추가한다(#496, additive — 기존
    엔드포인트는 변경되지 않는다). run 실행 과정(submitted/started/finished/
    failed, source fetch started/completed/failed, medallion
    stage(bronze/silver/gold/export) started/completed/failed, quality
    checkpoint)을 raw logger parsing 없이 append-only structured event
    timeline으로 조회한다. `limit`(기본 200, 상한 1000)과 `tail`로 bounded
    query를 제공하며, 반환은 항상 chronological ascending이다. event의
    ordering identifier(`seq`)는 store가 append 시점에 부여하는 monotonic
    값이라 병렬 source 간 실제 append 순서를 손실 없이 보존한다. Monitoring
    (#516, `GET /monitoring/summary`·`GET /monitoring/builds`)은 시스템/집계
    observability를 다루고, 이 endpoint는 단일 run의 event만 다룬다 — 역할이
    분리되어 있다. v1.15.0은 `/catalog` 응답(CatalogDataset)에 탐색용
    metadata(`description`/`tags`/`source_url`/`representation`/`operations`/
    `query_support`)를 추가한다(#490, additive — 기존 필드는 유지되며
    raw_metadata는 노출하지 않는다).
    v1.16.0은 async build job 표면을 계약에 문서화한다 — `POST /builds`(202
    접수/200 idempotent 재제출(#513)/409 완료 run 충돌/429 queue full)와
    `GET /builds/{run_id}`(잡 상태 polling, 성공 잡의 최종 build 응답 포함)을
    추가한다(#480, additive). 잡 상태 조회에 ownership 게이트를 적용해
    cross-owner의 build 출력 노출을 차단한다(behavioral tightening — 이전에는
    미검사였음).
    v1.17.0은 build publish 표면을 추가한다(#491) — `GET /builds/{run_id}/publish/readiness`(ready/blockers/warnings)와 `POST /builds/{run_id}/publish`(idempotent publish receipt, TOCTOU 재검사)를 추가한다(additive). HTTP target은 `huggingface`로 한정한다.
    v1.21.0은 publish 감사 로그 조회를 추가한다(#563, additive) — `GET /builds/{run_id}/publish/audit`(reconcile/reset 이력, 소유권 게이트, receipt 삭제 후에도 조회).
    v1.22.0은 `GET /datasets` 응답에 `total`을 추가한다(#488 후속, additive — 기존 필드/동작은 변경되지 않는다). `total`은 canonical dataset grouping과 ownership 필터를 적용한 뒤 pagination(limit) 이전의 접근 가능한 distinct dataset_id 개수이며, Studio Home의 DATASETS KPI가 `datasets` 길이나 limit을 total로 오인하지 않도록 authoritative aggregate를 제공한다. 같은 릴리스 범위에서 `/catalog` 응답의 `CatalogDataset`에 `request_parameters`와 `application`도 추가한다(additive — 기존 필드/동작은 변경되지 않는다). `raw_metadata`의 요청 파라미터 설명을 secret-free allowlist(`name`/`required`/`description`/`example`)로 직렬화한 것으로, `serviceKey` 등 시크릿 파라미터는 제외된다. Add Data가 선택한 Dataset의 필수 요청 파라미터를 사전에 안내하는 데 쓴다. metadata가 없는 dataset은 빈 배열. `application`은 공공데이터포털처럼 API Key 발급과 Dataset별 활용신청이 별개일 수 있는 경우의 안내(`{required, url}`)로, `raw_metadata.application`에서 그대로 전달하며 없으면 null이다 — Studio는 신청 완료/승인 여부를 추측하지 않는다.
    v1.20.0은 HTTP publish target 어휘를 확장한다(#550, additive) — `kaggle`(packaging의 `dataset-metadata.json` `id`가 destination과 일치해야 하며 `public` option 허용)과 `local`(`KPUBDATA_BUILDER_LOCAL_PUBLISH_ROOT` 안의 상대 `owner/name` 경로만 허용). `GET /publish/readiness`의 `destination` query는 선택이다.
    v1.19.0은 publish receipt 운영 경로를 추가한다(#551, additive) — `GET /builds/{run_id}/publish/receipt`(상태 조회, 소유자 불일치는 404), `POST /builds/{run_id}/publish/reconcile`(원격 상태 확인 후 `succeeded` 확정 또는 reset으로 재게시 허용; 판단 불가 시 503로 무변경), `DELETE /builds/{run_id}/publish/receipt`(명시적 reset, 감사 로그 기록).
    v1.18.0은 ADR 0008의 협력적 취소(cooperative cancellation)를 구현한다 —
    `POST /builds/{run_id}/cancel`을 추가한다(#481, additive). `queued` job은
    worker가 runner를 실행하기 전에 곧바로 `cancelled`로 종결되고(파이프라인
    산출물 0개), `running` job은 `cancelling`을 거쳐 **안전한 stage
    경계**(Bronze/Silver/Gold 산출물이 기록된 뒤, 다음 stage 시작 전)에서
    `cancelled`로 종결된다. worker thread를 강제로 죽이지 않으며, 실행 중인
    stage를 중간에 끊지 않는다. 이미 `succeeded`/`failed`인 job이나 파이프라인이
    정상 종료로 확정한 job에 대한 취소 요청은 409다. `BuildJobStatus` 어휘는
    기존 값을 그대로 재사용하며 새 상태를 만들지 않는다. 부분 산출물은 기본적으로
    삭제하지 않고 partial manifest와 함께 보존한다 — `BuildManifest`에
    `status`(`ok`/`failed`/`cancelled`)와 `partial`을 additive로 추가해, 취소된
    run이 성공 run으로 오인되지 않고 실행되지 않은 stage가 성공으로 기록되지
    않는다. run event 어휘에는 `run_cancelled`가 추가되며(run당 정확히 한 번,
    terminal 확정 시점) `BuildSummary.status`에도 `cancelled`가 관측 가능해진다.
servers:
  - url: http://localhost:8000
    description: 로컬 builder 개발 서버

security:
  - ApiKeyAuth: []
  - bearerAuth: []

paths:
  /healthz:
    get:
      operationId: healthz
      summary: 무인증 liveness probe (버전·메타 정보 없이 status만)
      security: []
      responses:
        "200":
          description: 서비스 정상
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"

  /version:
    get:
      operationId: getVersion
      summary: Builder API 계약 버전 조회
      responses:
        "200":
          description: 서비스/계약 버전
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /catalog:
    get:
      operationId: getCatalog
      summary: provider/dataset 카탈로그 조회 (BL2, #416)
      responses:
        "200":
          description: 사용 가능한 provider와 dataset 목록
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatalogResponse"
              examples:
                PublicDataCatalog:
                  summary: 탐색용 metadata를 포함한 공공데이터 카탈로그
                  value:
                    providers:
                      - name: datago
                        datasets:
                          - name: air_quality
                            title: 대기오염정보
                            description: 측정소별 대기오염 물질 농도 조회
                            tags: [environment, air]
                            source_url: https://www.data.go.kr/data/15073861/openapi
                            representation: api_json
                            operations: [get, list]
                            query_support:
                              pagination: offset
                              filterable_fields: [station_name]
                              sortable_fields: []
                              time_range: true
                              max_page_size: 1000
                            requires_service_key: true
                            request_parameters:
                              - name: sidoName
                                required: true
                                description: 조회할 시·도
                                example: 서울
                            application:
                              required: true
                              url: https://www.data.go.kr/data/15073861/openapi.do
                          - name: village_fcst
                            title: 단기예보
                            description: null
                            tags: []
                            source_url: null
                            representation: api_json
                            operations: [list]
                            query_support: null
                            requires_service_key: true
                            request_parameters: []
                            application: null
                      - name: ecos
                        datasets:
                          - name: key_statistic_list
                            title: 주요 경제지표
                            description: 한국은행 주요 경제지표 시계열
                            tags: [economy]
                            source_url: null
                            representation: api_json
                            operations: [list]
                            query_support: null
                            requires_service_key: false
                            request_parameters: []
                            application: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "502":
          description: 카탈로그 조회 실패

  /providers:
    get:
      operationId: listProviders
      summary: 런타임 Provider와 현재 principal의 configured 상태 조회
      responses:
        "200":
          description: Provider 목록. credential 원문과 owner_id는 포함하지 않는다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
        "502":
          description: 런타임 Provider 카탈로그 조회 실패

  /providers/{provider}/status:
    get:
      operationId: getProviderStatus
      summary: 현재 principal credential로 Provider 연결 상태 확인
      parameters:
        - $ref: "#/components/parameters/ProviderName"
      responses:
        "200":
          description: 연결 상태 또는 not_configured 결과
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderTestResponse"
              examples:
                Connected:
                  summary: 저장된 credential로 연결 성공
                  value:
                    provider: datago
                    status: connected
                    configured: true
                    latency_ms: 84
                    checked_at: "2026-08-15T09:30:00+00:00"
                NotConfigured:
                  summary: credential이 아직 설정되지 않음
                  value:
                    provider: datago
                    status: not_configured
                    configured: false
                    latency_ms: 0
                    checked_at: "2026-08-15T09:30:00+00:00"
                ProviderTimeout:
                  summary: Provider 연결 제한 시간 초과
                  value:
                    provider: datago
                    status: failed
                    configured: true
                    latency_ms: 10003
                    checked_at: "2026-08-15T09:30:10+00:00"
                    error_category: timeout
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
        "404":
          description: 알려지지 않은 Provider
        "502":
          description: 런타임 Provider 카탈로그 조회 실패

  /providers/{provider}/test:
    post:
      operationId: testProviderConnection
      summary: 현재 principal credential로 lightweight connection test 실행
      parameters:
        - $ref: "#/components/parameters/ProviderName"
      responses:
        "200":
          description: 구조화된 connection test 결과
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderTestResponse"
              examples:
                Connected:
                  summary: lightweight 연결 시험 성공
                  value:
                    provider: datago
                    status: connected
                    configured: true
                    latency_ms: 91
                    checked_at: "2026-08-15T09:30:00+00:00"
                AuthenticationFailed:
                  summary: Provider가 credential을 거부함
                  value:
                    provider: datago
                    status: failed
                    configured: true
                    latency_ms: 72
                    checked_at: "2026-08-15T09:30:00+00:00"
                    error_category: auth
                    response_code: 401
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
        "404":
          description: 알려지지 않은 Provider
        "502":
          description: 런타임 Provider 카탈로그 조회 실패

  /providers/{provider}/credential:
    get:
      operationId: getProviderCredential
      summary: 현재 principal의 저장 credential 메타데이터 조회
      parameters:
        - $ref: "#/components/parameters/ProviderName"
      responses:
        "200":
          description: 원문을 제외한 credential 메타데이터
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderCredentialGetResponse"
              examples:
                Configured:
                  summary: credential 원문을 노출하지 않는 저장 상태
                  value:
                    configured: true
                    masked: "********"
                    updated_at: "2026-08-15T09:25:00+00:00"
                NotConfigured:
                  summary: 저장된 credential 없음
                  value:
                    configured: false
                    masked: null
                    updated_at: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
        "404":
          description: 알려지지 않은 Provider
        "502":
          description: 런타임 Provider 카탈로그 조회 실패
        "503":
          description: encrypted credential store가 구성되지 않음
    put:
      operationId: putProviderCredential
      summary: 현재 principal의 Provider credential 생성 또는 교체
      parameters:
        - $ref: "#/components/parameters/ProviderName"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderCredentialPutRequest"
            examples:
              ReplaceCredential:
                summary: 문서용 placeholder credential 저장
                value:
                  credential: replace-with-your-provider-key
      responses:
        "200":
          description: 저장된 credential 메타데이터. 원문은 echo하지 않는다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderCredentialMetadata"
              examples:
                Stored:
                  summary: 원문을 echo하지 않는 저장 결과
                  value:
                    provider: datago
                    configured: true
                    masked: "********"
                    updated_at: "2026-08-15T09:25:00+00:00"
        "400":
          description: credential 누락, 빈 값, 또는 허용되지 않은 필드
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
        "404":
          description: 알려지지 않은 Provider
        "502":
          description: 런타임 Provider 카탈로그 조회 실패
        "503":
          description: encrypted credential store가 구성되지 않음
    delete:
      operationId: deleteProviderCredential
      summary: 현재 principal의 저장 Provider credential 삭제
      parameters:
        - $ref: "#/components/parameters/ProviderName"
      responses:
        "200":
          description: credential 삭제 완료. 존재하지 않아도 idempotent하게 성공한다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderCredentialMetadata"
              examples:
                Deleted:
                  summary: idempotent 삭제 결과
                  value:
                    provider: datago
                    configured: false
                    masked: null
                    updated_at: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
        "404":
          description: 알려지지 않은 Provider
        "502":
          description: 런타임 Provider 카탈로그 조회 실패
        "503":
          description: encrypted credential store가 구성되지 않음

  /uploads:
    post:
      operationId: createUpload
      summary: kind="file" source(#498)용 파일 업로드
      description: >-
        요청 body는 JSON이 아니라 업로드할 파일의 raw bytes다(multipart 대신 동등한
        binary upload, #498). `format`/`encoding`/`filename`은 query parameter로
        전달한다. 서버가 즉시 파싱 가능성을 검증해(손상·빈 파일은 400) fail-fast하고,
        성공하면 서버가 발급한 불투명한 `upload_id`를 반환한다 — 이 값을 BuildSpec의
        `sources[].upload_id`로 참조한다(형식은 `sources[].format`/`encoding`과
        정확히 일치해야 한다). content는 요청 principal의 owner_id로 격리되며,
        원문 content를 다시 내려주는 API는 없다(메타데이터만 조회 가능).
      parameters:
        - name: format
          in: query
          required: true
          description: 업로드 content의 파싱 포맷
          schema:
            type: string
            enum: [csv, json, jsonl, parquet]
        - name: encoding
          in: query
          required: false
          description: 텍스트 포맷(csv/json/jsonl) 디코딩 인코딩. parquet은 무시된다.
          schema:
            type: string
            default: utf-8
        - name: filename
          in: query
          required: false
          description: >-
            표시 전용 원본 파일명. 파일시스템 경로로 쓰이지 않으며 응답의
            original_filename에 basename만 반영된다.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: 업로드 저장 완료
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadMetadata"
              examples:
                Uploaded:
                  summary: csv 업로드 성공
                  value:
                    upload_id: upl_0123456789abcdef0123456789abcdef
                    format: csv
                    encoding: utf-8
                    size_bytes: 1024
                    original_filename: trades.csv
                    created_at: "2026-08-16T09:00:00+00:00"
        "400":
          description: 빈 body, 손상된 content, 지원하지 않는 format, 또는 크기 초과
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "413":
          description: 업로드 크기가 서버 상한을 초과함
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /uploads/{upload_id}:
    get:
      operationId: getUpload
      summary: 업로드 메타데이터 조회 (#498, content는 응답에 포함하지 않음)
      parameters:
        - $ref: "#/components/parameters/UploadId"
      responses:
        "200":
          description: 안전한(secret-free) 업로드 메타데이터
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadMetadata"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 존재하지 않거나 principal 소유가 아닌 upload_id (구분해 노출하지 않음)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteUpload
      summary: 현재 principal 소유 업로드 삭제 (#498)
      parameters:
        - $ref: "#/components/parameters/UploadId"
      responses:
        "200":
          description: 삭제 완료
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadDeleteResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: stable principal을 확인할 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 존재하지 않거나 principal 소유가 아닌 upload_id (구분해 노출하지 않음)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /validate:
    post:
      operationId: validateSpec
      summary: BuildSpec 검증 (동기식)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SpecRequest"
            examples:
              SeoulAirQuality:
                summary: 서울 대기질 BuildSpec 검증
                value:
                  spec: |
                    dataset_id: seoul-air-quality
                    title: 서울 대기질 관측
                    description: 서울 지역 측정소별 대기오염 관측 데이터
                    sources:
                      - provider: datago
                        dataset: air_quality
                        alias: air
                        params:
                          sidoName: 서울
                    exports:
                      - kind: jsonl
                        output_path: artifacts/air-quality.jsonl
      responses:
        "200":
          description: 검증 통과
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateResponse"
              examples:
                ValidSpec:
                  summary: BuildSpec 검증 통과
                  value:
                    status: valid
                    dataset_id: seoul-air-quality
                    api_version: "1.8.0"
        "400":
          description: spec 파싱 실패 또는 검증 실패
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
              examples:
                MissingSources:
                  summary: 필수 sources가 없는 BuildSpec
                  value:
                    status: invalid
                    problems:
                      - sources must contain at least one source
                MalformedYaml:
                  summary: YAML 파싱 실패
                  value:
                    status: error
                    error: could not parse BuildSpec YAML
        "401":
          $ref: "#/components/responses/Unauthorized"

  /preview:
    post:
      operationId: previewBuild
      summary: 각 소스의 스키마와 샘플 행 산출 (파일 미기록, 동기식)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreviewRequest"
            examples:
              AirQualityPreview:
                summary: 최대 2행의 대기질 preview 요청
                value:
                  spec: |
                    dataset_id: seoul-air-quality
                    title: 서울 대기질 관측
                    description: 서울 지역 측정소별 대기오염 관측 데이터
                    sources:
                      - provider: datago
                        dataset: air_quality
                        alias: air
                    exports:
                      - kind: jsonl
                        output_path: artifacts/air-quality.jsonl
                  limit: 2
      responses:
        "200":
          description: preview 결과
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewResponse"
              examples:
                AirQualityPreview:
                  summary: 정규화된 스키마, 표본, 통계와 품질 결과
                  value:
                    dataset_id: seoul-air-quality
                    previews:
                      - source_key: air
                        status: ok
                        error: null
                        schema:
                          - name: station_name
                            dtype: String
                            nullable: false
                            unique_count: 2
                          - name: pm10_value
                            dtype: Int64
                            nullable: true
                            unique_count: 2
                        sample:
                          - station_name: 종로구
                            pm10_value: 31
                          - station_name: 중구
                            pm10_value: 28
                        total_rows: 25
                        statistics:
                          row_count: 25
                          null_counts:
                            station_name: 0
                            pm10_value: 1
                          duplicate_rate: 0.0
                        quality_results:
                          - source_key: air
                            category: row_count
                            rule: min_rows
                            column: null
                            status: pass
                            actual: 25
                            threshold: 1
                            affected_rows: null
                            evaluated_rows: null
                            detail: null
                        source_sample:
                          - station_name: 종로구
                            pm10_value: 31
                          - station_name: 중구
                            pm10_value: 28
                        sample_mode: first
                        diff_available: true
                        diffs: []
                        transform_summary:
                          changed_cells: 0
                          changed_rows: 0
                        diff_truncated: false
        "400":
          description: 잘못된 요청(spec 누락/검증 실패, limit 타입 오류 등)
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/Error"
                  - $ref: "#/components/schemas/ValidationError"
              examples:
                InvalidLimit:
                  summary: 양수가 아닌 preview limit
                  value:
                    error: "'limit' must be a positive integer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "502":
          description: principal credential로 Provider client를 생성할 수 없음

  /build:
    post:
      operationId: createBuild
      summary: 파이프라인 실행 및 결과 반환 (동기식)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BuildRequest"
            examples:
              BuildAirQuality:
                summary: 명시적인 안전한 run_id로 동기 build 실행
                value:
                  spec: |
                    dataset_id: seoul-air-quality
                    title: 서울 대기질 관측
                    description: 서울 지역 측정소별 대기오염 관측 데이터
                    sources:
                      - provider: datago
                        dataset: air_quality
                        alias: air
                    exports:
                      - kind: jsonl
                        output_path: artifacts/air-quality.jsonl
                  run_id: air-quality-20260815
      responses:
        "200":
          description: 모든 소스 성공
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildSuccessResponse"
              examples:
                BuildCompleted:
                  summary: Bronze, Silver, Gold를 완료한 동기 build
                  value:
                    status: ok
                    run_id: air-quality-20260815
                    outcomes:
                      - source_key: air
                        status: ok
                        stages_completed: [bronze, silver, gold]
                        error: null
                    manifest: build/air-quality-20260815/manifest.json
                    api_version: "1.8.0"
        "400":
          description: 잘못된 요청(spec 누락/검증 실패, run_id 타입/경로 안전성 오류 등)
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/Error"
                  - $ref: "#/components/schemas/ValidationError"
              examples:
                UnsafeRunId:
                  summary: 경로 구분자를 포함한 run_id
                  value:
                    error: run_id must be a path-safe single segment
        "401":
          $ref: "#/components/responses/Unauthorized"
        "502":
          description: >-
            하나 이상의 소스 fetch/stage 실패. upstream 소스 의존 실패이며 매니페스트는
            partial 정책으로 남는다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildFailureResponse"
              examples:
                ProviderFetchFailed:
                  summary: upstream fetch 실패 후 partial manifest를 남긴 build
                  value:
                    status: failed
                    run_id: air-quality-20260815
                    outcomes:
                      - source_key: air
                        status: failed
                        stages_completed: []
                        error: provider request failed
                    manifest: build/air-quality-20260815/manifest.json
                    api_version: "1.8.0"
                    error: provider request failed

  /artifacts/{run_id}:
    get:
      operationId: listBuildArtifacts
      summary: 실행 워크스페이스의 산출물 파일 목록 조회
      parameters:
        - $ref: "#/components/parameters/RunId"
      responses:
        "200":
          description: artifact 파일 목록
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactsResponse"
              examples:
                BuildArtifacts:
                  summary: run 디렉터리 기준 상대 경로 목록
                  value:
                    run_id: air-quality-20260815
                    files:
                      - buildspec.yaml
                      - manifest.json
                      - artifacts/air-quality.jsonl
                      - silver/air/table.parquet
        "400":
          description: 경로 안전하지 않은 run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: 해당 run을 찾을 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                RunNotFound:
                  summary: 존재하지 않는 run
                  value:
                    error: "run not found: missing-run"

  /builds/{run_id}:
    get:
      operationId: getBuildJob
      summary: 비동기 build job 상태 polling (#482, #480)
      description: >-
        active(queued/running) 또는 terminal(succeeded/failed) async build job의
        상태를 반환한다. 성공한 잡은 `response`에 최종 build 응답 본문을 포함한다.
        active 구간(run directory/manifest가 아직 없는)도 async job registry로
        인지하며, ownership 판정은 `/builds/{run_id}/events`와 동일한
        canonical 규칙(manifest 우선, active는 registry snapshot의 stable
        owner_id)을 따른다. 잡 상태가 404면 이후에도 상태가 생성되지 않는다 —
        registry는 프로세스 메모리 상주라 서버 재시작 후 active 잡은 조회되지
        않는다(#482 restart semantics).
      parameters:
        - $ref: "#/components/parameters/RunId"
      responses:
        "200":
          description: build job 상태
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildJob"
              examples:
                Running:
                  summary: 실행 중인 job
                  value:
                    run_id: run-0123456789abcdef
                    status: running
                    created_at: "2026-08-16T09:00:00+00:00"
                    updated_at: "2026-08-16T09:00:01+00:00"
                    created_by: oidc:user-a
                Succeeded:
                  summary: 완료된 job(최종 build 응답 포함)
                  value:
                    run_id: run-0123456789abcdef
                    status: succeeded
                    created_at: "2026-08-16T09:00:00+00:00"
                    updated_at: "2026-08-16T09:00:07+00:00"
                    created_by: oidc:user-a
                    response:
                      run_id: run-0123456789abcdef
                      status: ok
        "400":
          description: 안전하지 않은 run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: run 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 알려지지 않은 run(완료 run 포함, registry에 없음)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /builds/{run_id}/cancel:
    post:
      operationId: cancelBuildJob
      summary: 비동기 build job 협력적 취소 요청 (#481, ADR 0008)
      description: >-
        active(`queued`/`running`) async build job의 취소를 요청한다. worker
        thread를 강제로 종료하지 않는다 — `queued` job은 worker가 runner를
        실행하기 전에 곧바로 `cancelled`로 종결되고(파이프라인 산출물 0개),
        `running` job은 즉시 `cancelling`으로 표시된 뒤 **안전한 stage
        경계**(Bronze/Silver/Gold 산출물이 기록된 뒤 다음 stage를 시작하기 전)에서
        `cancelled`로 종결된다. 실행 중인 stage를 중간에 끊지 않으므로 응답
        시점에 job이 아직 `cancelling`일 수 있다 — 최종 상태는
        `GET /builds/{run_id}`로 확인한다. 취소 시점까지 만들어진 산출물은
        삭제하지 않고 `partial: true` manifest(`status: cancelled`)와 함께
        보존한다. 이미 `cancelling`/`cancelled`인 job에 다시 요청해도 현재
        상태를 그대로 200으로 돌려주는 멱등 연산이다. ownership 판정은
        `GET /builds/{run_id}`와 동일한 canonical 규칙(manifest 우선, active는
        registry snapshot의 stable owner_id)을 따른다.
      parameters:
        - $ref: "#/components/parameters/RunId"
      responses:
        "200":
          description: >-
            취소가 접수됨(또는 이미 취소된 job에 대한 멱등 재요청). 본문은 전이
            직후의 job 스냅샷이다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildJob"
              examples:
                QueuedCancelled:
                  summary: 실행 전 queued job이 즉시 종결 취소됨
                  value:
                    run_id: run-0123456789abcdef
                    status: cancelled
                    created_at: "2026-08-16T09:00:00+00:00"
                    updated_at: "2026-08-16T09:00:02+00:00"
                    created_by: oidc:user-a
                RunningCancelling:
                  summary: running job에 취소를 요청함(다음 안전 경계에서 종결)
                  value:
                    run_id: run-0123456789abcdef
                    status: cancelling
                    created_at: "2026-08-16T09:00:00+00:00"
                    updated_at: "2026-08-16T09:00:03+00:00"
                    created_by: oidc:user-a
        "400":
          description: 안전하지 않은 run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: run 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 알려지지 않은 run(registry에 없음)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: >-
            더 이상 취소할 수 없는 job — 이미 `succeeded`/`failed`로 끝났거나,
            파이프라인이 마지막 안전 경계를 지나 정상 종료로 확정했다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /builds/{run_id}/manifest:
    get:
      operationId: getBuildManifest
      summary: 실행 워크스페이스의 manifest.json 본문 조회
      parameters:
        - $ref: "#/components/parameters/RunId"
      responses:
        "200":
          description: manifest.json 본문
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildManifest"
              examples:
                CompletedManifest:
                  summary: secret과 내부 owner_id가 없는 완료 manifest
                  value:
                    build_id: air-quality-20260815
                    started_at: "2026-08-15T09:30:00+00:00"
                    finished_at: "2026-08-15T09:30:07+00:00"
                    schema_version: "1.0"
                    inputs: [air]
                    outputs: [artifacts/air-quality.jsonl]
                    warnings: []
                    errors: []
                    row_counts:
                      air: 25
                    inputs_fingerprint: "sha256:example-fingerprint"
                    created_by: "oidc:example-user"
                    quality_results:
                      air: []
                    schema_drift:
                      air: []
        "400":
          description: 경로 안전하지 않은 run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: 해당 run 또는 manifest를 찾을 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                ManifestNotFound:
                  summary: manifest가 없는 run
                  value:
                    error: "manifest not found: missing-run"
        "500":
          description: manifest 파일을 읽거나 JSON으로 해석할 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /builds/{run_id}/spec:
    get:
      operationId: getBuildSpecSnapshot
      summary: 실행에 사용한 canonical BuildSpec snapshot 조회
      parameters:
        - $ref: "#/components/parameters/RunId"
      responses:
        "200":
          description: redaction된 canonical buildspec.yaml 본문과 SHA-256 digest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildSpecSnapshotResponse"
              examples:
                CanonicalSnapshot:
                  summary: canonical YAML과 redaction 후 digest
                  value:
                    run_id: air-quality-20260815
                    spec: |
                      dataset_id: seoul-air-quality
                      title: 서울 대기질 관측
                      description: 서울 지역 측정소별 대기오염 관측 데이터
                      sources:
                        - provider: datago
                          dataset: air_quality
                          alias: air
                      exports:
                        - kind: jsonl
                          output_path: artifacts/air-quality.jsonl
                    spec_digest: "sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
        "400":
          description: 경로 안전하지 않은 run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: 해당 run의 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 해당 run이 없거나 legacy run에 snapshot이 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                SnapshotNotFound:
                  summary: legacy run에 canonical snapshot이 없음
                  value:
                    error: "BuildSpec snapshot unavailable for run: legacy-run"
        "500":
          description: snapshot 파일을 읽거나 UTF-8로 해석할 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /builds:
    post:
      operationId: submitBuild
      summary: 비동기 build job 제출 (#482, ADR 0008 #480)
      description: >-
        BuildSpec을 검증해 async build job으로 큐잉하고 즉시 초기 상태를 반환한다.
        동일 run_id의 active job이 있으면 idempotent하게 기존 잡 상태를 200으로
        반환하고(#513), 이미 완료된 run_id면 409, queue가 가득 차면 429를
        반환한다. 잡의 실행 결과는 `GET /builds/{run_id}`로 polling하고,
        협력적 취소는 `POST /builds/{run_id}/cancel`(#481)로 요청한다.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [spec]
              properties:
                spec:
                  type: string
                  description: BuildSpec을 YAML로 직렬화한 문자열
                run_id:
                  type: string
                  description: >-
                    클라이언트 지정 run_id(선택). 생략하면 서버가 발급한다.
                    안전한 path segment여야 한다.
            examples:
              SubmitBuild:
                summary: 비동기 build 제출
                value:
                  spec: |
                    dataset_id: seoul-air-quality
                    title: 서울 대기질 관측
                    description: 서울 지역 측정소별 대기오염 관측 데이터
                    sources:
                      - provider: datago
                        dataset: air_quality
                        alias: air
                    exports:
                      - kind: jsonl
                        output_path: artifacts/air-quality.jsonl
      responses:
        "202":
          description: build job이 접수되어 큐잉됨
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildJob"
              examples:
                Accepted:
                  summary: 신규 접수된 queued job
                  value:
                    run_id: run-0123456789abcdef
                    status: queued
                    created_at: "2026-08-16T09:00:00+00:00"
                    updated_at: "2026-08-16T09:00:00+00:00"
                    created_by: oidc:user-a
        "200":
          description: 동일 run_id의 active job이 이미 존재함(idempotent 재제출, #513)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildJob"
        "400":
          description: 잘못된 spec/run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: run_id가 이미 완료된 run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: async build queue가 가득 참
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: job 접수 실패(event 기록/큐잉 불가 — job은 생성되지 않음)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: listBuilds
      summary: "빌드 이력 목록 조회 (최신 수정 시각 기준 내림차순)"
      parameters:
        - name: limit
          in: query
          required: false
          description: "반환할 최대 빌드 수 (기본값: 50)"
          schema:
            type: integer
            minimum: 1
            default: 50
      responses:
        "200":
          description: 빌드 이력 목록
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildsResponse"
              examples:
                RecentBuilds:
                  summary: 최신 수정 시각 순 build 목록
                  value:
                    builds:
                      - run_id: air-quality-20260815
                        status: ok
                        started_at: "2026-08-15T09:30:00+00:00"
                        finished_at: "2026-08-15T09:30:07+00:00"
                      - run_id: air-quality-20260814
                        status: failed
                        started_at: "2026-08-14T09:30:00+00:00"
                        finished_at: "2026-08-14T09:30:02+00:00"
        "400":
          description: 잘못된 limit 파라미터
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                InvalidLimit:
                  summary: 양수가 아닌 목록 limit
                  value:
                    error: limit must be a positive integer
        "401":
          $ref: "#/components/responses/Unauthorized"

  /datasets:
    get:
      operationId: listDatasets
      summary: >-
        Built dataset 목록 조회 (dataset_id별 grouping, #488). 동일 dataset_id의
        여러 run을 접근 가능한 latest run 기준으로 하나의 dataset으로 묶는다.
        buildspec.yaml snapshot이 없는 legacy run은 grouping 대상에서 제외된다.
      parameters:
        - name: limit
          in: query
          required: false
          description: "반환할 최대 dataset 수 (기본값: 50)"
          schema:
            type: integer
            minimum: 1
            default: 50
      responses:
        "200":
          description: built dataset 목록 (latest run 기준 요약)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatasetsResponse"
              examples:
                BuiltDatasets:
                  summary: dataset_id별 latest accessible run 요약
                  value:
                    total: 1
                    datasets:
                      - dataset_id: seoul-air-quality
                        title: 서울 대기질 관측
                        sources:
                          - provider: datago
                            dataset: air_quality
                            alias: air
                        latest_run_id: air-quality-20260815
                        status: ok
                        updated_at: "2026-08-15T09:30:07+00:00"
                        row_counts:
                          air: 25
                        total_row_count: 25
                        stages:
                          air:
                            bronze: completed
                            silver: completed
                            gold: completed
                        quality: null
        "400":
          description: 잘못된 limit 파라미터
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /datasets/{dataset_id}:
    get:
      operationId: getDataset
      summary: 단일 built dataset의 canonical 요약 조회 (latest accessible run 기준, #488)
      parameters:
        - $ref: "#/components/parameters/DatasetId"
      responses:
        "200":
          description: dataset 요약 (title/sources/latest run/row_counts/stage 상태 등)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatasetDetailResponse"
              examples:
                AirQualityDataset:
                  summary: canonical snapshot과 manifest에서 만든 dataset 상세
                  value:
                    dataset_id: seoul-air-quality
                    title: 서울 대기질 관측
                    sources:
                      - provider: datago
                        dataset: air_quality
                        alias: air
                    latest_run_id: air-quality-20260815
                    status: ok
                    updated_at: "2026-08-15T09:30:07+00:00"
                    row_counts:
                      air: 25
                    total_row_count: 25
                    stages:
                      air:
                        bronze: completed
                        silver: completed
                        gold: completed
                    quality: null
                    run_count: 2
        "400":
          description: dataset_id가 비어 있음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            접근 가능한 run이 없는 dataset_id. dataset_id가 실제로 존재하지 않는
            경우와 타 사용자 소유 run만 있는 경우를 구분해 노출하지 않는다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                DatasetNotFound:
                  summary: 접근 가능한 run이 없는 dataset
                  value:
                    error: "dataset not found: unknown-dataset"

  /datasets/{dataset_id}/runs:
    get:
      operationId: listDatasetRuns
      summary: dataset_id의 접근 가능한 run history 조회 (최신순, #488)
      parameters:
        - $ref: "#/components/parameters/DatasetId"
        - name: limit
          in: query
          required: false
          description: "반환할 최대 run 수 (기본값: 50)"
          schema:
            type: integer
            minimum: 1
            default: 50
      responses:
        "200":
          description: dataset의 run history (newest first, 타 사용자 run 제외)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatasetRunsResponse"
              examples:
                AirQualityRuns:
                  summary: 최신순 canonical run history
                  value:
                    dataset_id: seoul-air-quality
                    runs:
                      - run_id: air-quality-20260815
                        status: ok
                        started_at: "2026-08-15T09:30:00+00:00"
                        finished_at: "2026-08-15T09:30:07+00:00"
                        spec_digest: "sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
                        created_by: "oidc:example-user"
                      - run_id: air-quality-20260814
                        status: failed
                        started_at: "2026-08-14T09:30:00+00:00"
                        finished_at: "2026-08-14T09:30:02+00:00"
                        spec_digest: null
                        created_by: null
        "400":
          description: dataset_id가 비어 있거나 limit이 잘못됨
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: 접근 가능한 run이 없는 dataset_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                DatasetNotFound:
                  summary: run history를 조회할 수 없는 dataset
                  value:
                    error: "dataset not found: unknown-dataset"

  /builds/{run_id}/events:
    get:
      operationId: getBuildEvents
      summary: >-
        run의 append-only structured event timeline 조회 (#496). raw logger를
        파싱하지 않고, 실제 실행 boundary(run/source fetch/medallion
        stage/quality checkpoint)에서 남긴 구조화된 event만 반환한다. 반환은
        항상 chronological ascending이다 — `tail=true`도 최신 `limit`개를
        고르되 결과 자체는 뒤집지 않는다. 실패했다고 이전 성공 event를
        지우지 않으므로(append-only), partial run에서도 마지막 정상 event와
        실패 event를 함께 확인할 수 있다.
      parameters:
        - $ref: "#/components/parameters/RunId"
        - name: limit
          in: query
          required: false
          description: "반환할 최대 event 수 (기본값: 200, 상한: 1000)"
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 200
        - name: tail
          in: query
          required: false
          description: >-
            true면 run 시작부터가 아니라 가장 최근 `limit`개를 선택한다(반환
            순서는 여전히 chronological ascending).
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: run_id와 chronological ascending으로 정렬된 event 목록
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildEventsResponse"
              examples:
                CompletedRun:
                  summary: 단일 source가 Bronze→Silver→Gold→Export까지 모두 성공한 run
                  value:
                    run_id: air-quality-20260815
                    events:
                      - seq: 1
                        timestamp: "2026-08-15T09:30:00+00:00"
                        run_id: air-quality-20260815
                        event: run_started
                        status: ok
                        source_key: null
                        stage: null
                        message: pipeline execution started
                        metrics: null
                      - seq: 2
                        timestamp: "2026-08-15T09:30:01+00:00"
                        run_id: air-quality-20260815
                        event: stage_completed
                        status: ok
                        source_key: air
                        stage: bronze
                        message: Bronze written
                        metrics: {records: 84321}
                      - seq: 3
                        timestamp: "2026-08-15T09:30:02+00:00"
                        run_id: air-quality-20260815
                        event: run_finished
                        status: ok
                        source_key: null
                        stage: null
                        message: build completed
                        metrics: null
                PartialFailure:
                  summary: 한 source가 bronze fetch에서 실패한 run — 이전 event는 남아 있다
                  value:
                    run_id: air-quality-partial
                    events:
                      - seq: 1
                        timestamp: "2026-08-15T09:30:00+00:00"
                        run_id: air-quality-partial
                        event: run_started
                        status: ok
                        source_key: null
                        stage: null
                        message: pipeline execution started
                        metrics: null
                      - seq: 2
                        timestamp: "2026-08-15T09:30:01+00:00"
                        run_id: air-quality-partial
                        event: source_fetch_failed
                        status: fail
                        source_key: air
                        stage: null
                        message: "pipeline failed for source 'air'"
                        metrics: null
                      - seq: 3
                        timestamp: "2026-08-15T09:30:01+00:00"
                        run_id: air-quality-partial
                        event: run_failed
                        status: fail
                        source_key: null
                        stage: null
                        message: 1 source(s) failed
                        metrics: null
        "400":
          description: 경로 안전하지 않은 run_id, 잘못된 limit, 또는 잘못된 tail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: 해당 run의 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 해당 run을 찾을 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /builds/{run_id}/publish/readiness:
    get:
      operationId: getPublishReadiness
      summary: >-
        run이 target에 게시 가능한지 side-effect-free로 확인한다 (#491). Publisher를
        호출하거나 원격 dataset을 만들지 않는다. `ready`는 항상 `blockers`가
        비어 있는지로 deterministic하게 계산되며, `warnings`는 게시 자체를
        막지 않는다. `POST .../publish`가 같은 판정을 서버에서 다시 수행하므로,
        이 응답을 신뢰해 blocker 재검증을 건너뛰면 안 된다(TOCTOU).
      parameters:
        - $ref: "#/components/parameters/RunId"
        - name: target
          in: query
          required: true
          description: 게시 대상 (기존 publisher registry의 HTTP-safe 부분집합)
          schema:
            type: string
            enum: [huggingface]
      responses:
        "200":
          description: readiness 판정
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublishReadinessResponse"
              examples:
                Ready:
                  summary: Gold + license + credential이 모두 준비된 run
                  value:
                    run_id: air-quality-20260815
                    target: huggingface
                    ready: true
                    blockers: []
                    warnings: []
                Blocked:
                  summary: license 미선언 + credential 미설정
                  value:
                    run_id: air-quality-20260815
                    target: huggingface
                    ready: false
                    blockers:
                      - code: license_missing
                        message: >-
                          BuildSpec.license must be declared before this dataset
                          can be published (#443)
                      - code: credential_unavailable
                        message: no server-side credential is configured for target 'huggingface'
                    warnings: []
        "400":
          description: 경로 안전하지 않은 run_id, target 누락, 또는 지원하지 않는 target
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                UnsupportedTarget:
                  summary: local은 HTTP publish target에서 제외된다(#491)
                  value:
                    error: "target 'local' is not available over the publish HTTP API"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: 해당 run의 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 해당 run을 찾을 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /builds/{run_id}/publish:
    post:
      operationId: publishBuild
      summary: >-
        readiness와 완전히 같은 검사를 서버에서 다시 수행한 뒤 기존 Publisher를
        호출한다 (#491). blocker가 하나라도 있으면 Publisher는 호출되지 않는다
        (호출 횟수 0). 새 Publisher를 만들지 않으며, `publishers.PUBLISHER_REGISTRY`
        (huggingface)의 기존 구현을 그대로 재사용한다. `destination`은
        filesystem path로 해석되지 않고 target별 canonical identifier(`owner/name`)
        형태만 허용된다.
      parameters:
        - $ref: "#/components/parameters/RunId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublishRequest"
            examples:
              HuggingFace:
                value:
                  target: huggingface
                  destination: kpubdata/air-quality
                  options:
                    private: true
      responses:
        "200":
          description: 게시 성공
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublishResponse"
              examples:
                Published:
                  value:
                    run_id: air-quality-20260815
                    target: huggingface
                    publisher: huggingface
                    destination: kpubdata/air-quality
                    reference: https://huggingface.co/datasets/kpubdata/air-quality
                    artifact_count: 3
                    status: ok
        "400":
          description: >-
            경로 안전하지 않은 run_id, 잘못된 요청 본문, 지원하지 않는 target,
            잘못된 destination 형태, 또는 target이 지원하지 않는 option
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                InvalidDestination:
                  summary: destination이 filesystem path처럼 보임(path traversal)
                  value:
                    error: "'destination' must not contain path traversal segments"
                UnknownOption:
                  summary: huggingface가 지원하지 않는 option
                  value:
                    error: "unsupported option for target 'huggingface': 'visibility'"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: 해당 run의 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 해당 run을 찾을 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: >-
            readiness blocker가 있거나, 동일 operation이 진행/결과 불명 상태이거나,
            같은 run/target/destination이 다른 options로 이미 게시됨
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/PublishBlockedResponse"
                  - $ref: "#/components/schemas/Error"
              examples:
                Blocked:
                  value:
                    error: "run is not ready to publish to 'huggingface'"
                    blockers:
                      - code: gold_unavailable
                        message: no successful Gold output is available for this run
                InProgress:
                  value:
                    error: publish operation is already in progress
                    code: publish_in_progress
                UnknownState:
                  value:
                    error: publish operation outcome is unknown; automatic retry is blocked
                    code: publish_state_unknown
                Conflict:
                  value:
                    error: this run and destination were already published with different options
                    code: publish_conflict
        "502":
          description: >-
            기존 Publisher 호출이 실패함(원격 인증/네트워크/provider 오류).
            Publisher가 던진 예외의 원문 메시지(secret/절대 filesystem path를
            담을 수 있음)는 절대 그대로 반환하지 않는다(#491) — 항상 이
            stable generic 메시지와 code만 나간다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                RemoteFailure:
                  value:
                    error: "publish failed due to an unexpected error"
                    code: publish_failed

  /builds/{run_id}/stages:
    get:
      operationId: listBuildStages
      summary: run에 알려진 모든 source의 Bronze/Silver/Gold 상태 조회 (#488)
      parameters:
        - $ref: "#/components/parameters/RunId"
      responses:
        "200":
          description: source별 Bronze/Silver/Gold 상태 목록
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunStagesResponse"
              examples:
                CompletedStages:
                  summary: 단일 source의 모든 medallion stage 완료
                  value:
                    run_id: air-quality-20260815
                    sources:
                      - source_key: air
                        bronze:
                          status: completed
                          available: true
                        silver:
                          status: completed
                          available: true
                        gold:
                          status: completed
                          available: true
        "400":
          description: 경로 안전하지 않은 run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: 해당 run의 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 해당 run을 찾을 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /builds/{run_id}/stages/{stage}:
    get:
      operationId: getBuildStageDetail
      summary: >-
        단일 source의 단일 stage(bronze/silver/gold)에 대한 안전한 summary/preview
        조회 (#488). raw fetch_params, export options, credential, 절대 경로는
        어디에도 담기지 않는다. Silver sample은 build 시점에 이미 persist된 상한
        (기본 5행) 안에서만 반환되며 parquet 전체를 읽지 않는다.
      parameters:
        - $ref: "#/components/parameters/RunId"
        - $ref: "#/components/parameters/Stage"
        - name: source
          in: query
          required: true
          description: 조회할 source_key (run에 알려진 source여야 함)
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: "Silver sample 행 수 상한 (기본값: 5, 다른 stage에서는 무시됨)"
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 5
      responses:
        "200":
          description: stage summary/preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StageDetailResponse"
              examples:
                BronzeDetail:
                  summary: credential과 fetch params가 제거된 Bronze summary
                  value:
                    run_id: air-quality-20260815
                    stage: bronze
                    source_key: air
                    status: completed
                    available: true
                    provider: datago
                    dataset: air_quality
                    fetched_at: "2026-08-15T09:30:02+00:00"
                    record_count: 25
                SilverDetail:
                  summary: persist된 bounded Silver sample
                  value:
                    run_id: air-quality-20260815
                    stage: silver
                    source_key: air
                    status: completed
                    available: true
                    row_count: 25
                    schema:
                      - name: station_name
                        dtype: String
                        nullable: false
                        unique_count: 25
                      - name: pm10_value
                        dtype: Int64
                        nullable: true
                        unique_count: 18
                    statistics:
                      row_count: 25
                      null_counts:
                        station_name: 0
                        pm10_value: 1
                      duplicate_rate: 0.0
                    validation:
                      ok: true
                      problems: []
                    sample:
                      - station_name: 종로구
                        pm10_value: 31
                      - station_name: 중구
                        pm10_value: 28
                GoldDetail:
                  summary: export options와 내부 경로가 없는 Gold summary
                  value:
                    run_id: air-quality-20260815
                    stage: gold
                    source_key: air
                    status: completed
                    available: true
                    row_count: 25
                    columns: [station_name, pm10_value]
                    splits: null
                    exports:
                      - kind: jsonl
                    sample: null
                    sample_available: false
        "400":
          description: >-
            경로 안전하지 않은 run_id/source, 잘못된 stage 이름, source 누락,
            또는 잘못된 limit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                MissingSource:
                  summary: 필수 source query parameter 누락
                  value:
                    error: source query parameter is required
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: 해당 run의 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 해당 run이 없거나, source가 이 run에 알려지지 않음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /datasets/{dataset_id}/quality/history:
    get:
      operationId: getDatasetQualityHistory
      summary: >-
        dataset_id의 접근 가능한 run들에 대한 Quality PASS/WARN/FAIL 집계 이력
        (#486). dataset→run 조회는 #488과 동일한 helper를 재사용하며, ownership도
        동일 semantics를 공유한다.
      parameters:
        - $ref: "#/components/parameters/DatasetId"
        - name: limit
          in: query
          required: false
          description: "반환할 최대 run 수 (기본값: 30)"
          schema:
            type: integer
            minimum: 1
            default: 30
      responses:
        "200":
          description: run별 quality 집계 이력 (newest first)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DatasetQualityHistoryResponse"
              examples:
                AirQualityHistory:
                  summary: 평가 건수와 검증 행 수를 분리한 quality 이력
                  value:
                    dataset_id: seoul-air-quality
                    runs:
                      - run_id: air-quality-20260815
                        timestamp: "2026-08-15T09:30:07+00:00"
                        status: ok
                        pass_count: 2
                        warn_count: 1
                        fail_count: 0
                        evaluated_checks: 3
                        rule_pass_rate: 0.6666666666666666
                        validated_rows: 25
                      - run_id: air-quality-legacy
                        timestamp: null
                        status: ok
                        pass_count: 0
                        warn_count: 0
                        fail_count: 0
                        evaluated_checks: 0
                        rule_pass_rate: null
                        validated_rows: null
        "400":
          description: dataset_id가 비어 있거나 limit이 잘못됨
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: 접근 가능한 run이 없는 dataset_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                DatasetNotFound:
                  summary: quality history가 없는 dataset
                  value:
                    error: "dataset not found: unknown-dataset"

  /builds/{run_id}/quality:
    get:
      operationId: getBuildQuality
      summary: >-
        run의 구조화된 Quality 결과와 schema drift 조회 (#486). manifest에 저장된
        source_key별 QualityCheckResult(PASS 포함)와 SchemaDriftFinding을 그대로
        반환한다 — 로그를 파싱하지 않고 JSON으로 조회 가능하다. `availability`가
        `available`/`partial`/`unavailable`를 구분하므로, 빈 `quality_results`가
        "0건 평가"인지 "애초에 계산된 적 없음"인지 클라이언트가 판별할 수 있다(#514).
      parameters:
        - $ref: "#/components/parameters/RunId"
      responses:
        "200":
          description: run의 quality_results/schema_drift
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildQualityResponse"
              examples:
                EvaluatedQuality:
                  summary: PASS와 WARN 및 schema drift를 포함한 run quality
                  value:
                    run_id: air-quality-20260815
                    availability: available
                    evaluated_checks: 2
                    quality_results:
                      air:
                        - source_key: air
                          category: row_count
                          rule: min_rows
                          column: null
                          status: pass
                          actual: 25
                          threshold: 10
                          affected_rows: null
                          evaluated_rows: null
                          detail: null
                        - source_key: air
                          category: missing
                          rule: max_null_ratio
                          column: pm10_value
                          status: warn
                          actual: 0.04
                          threshold: 0.01
                          affected_rows: 1
                          evaluated_rows: 25
                          detail: null
                    schema_drift:
                      air:
                        - kind: column_added
                          column: station_code
                          detail: column added since previous successful run
                LegacyQualityUnavailable:
                  summary: quality 결과가 없던 legacy run
                  value:
                    run_id: air-quality-legacy
                    availability: unavailable
                    evaluated_checks: 0
                    quality_results: {}
                    schema_drift: {}
        "400":
          description: 경로 안전하지 않은 run_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: 해당 run의 소유자가 아님
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: 해당 run을 찾을 수 없음
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                RunNotFound:
                  summary: manifest가 없는 run
                  value:
                    error: "manifest not found: missing-run"

  /quality/summary:
    get:
      operationId: getQualitySummary
      summary: >-
        최근 window(24h) cross-run quality aggregate (#486 후속, additive — API
        1.22.0). 접근 가능한 run들의 structured quality를 PASS/WARN/FAIL run 수로
        요약한다. Studio Home의 "QUALITY WARN (24H)" KPI가 임의 숫자 합성 없이
        authoritative aggregate를 읽도록 하는 bounded 집계다. 개별 run의
        `quality_results`/dataset/owner는 노출하지 않는다 — 그건 per-run
        `GET /builds/{run_id}/quality`의 몫이다. 시스템 observability
        (`GET /monitoring/summary`·`GET /monitoring/builds`, #516)와 도메인
        quality를 한 응답에 섞지 않는다. ENFORCE_OWNERSHIP+oidc principal이면
        본인이 접근 가능한 run만 집계한다(#505). legacy run(BuildSpec snapshot
        없음)은 structured quality가 없으므로 자연히 제외된다.
      parameters:
        - name: window
          in: query
          required: false
          description: "집계 기간. 현재 24h만 지원 (기본값: 24h)"
          schema:
            type: string
            enum: [24h]
            default: 24h
      responses:
        "200":
          description: 최근 window의 PASS/WARN/FAIL run 수
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QualitySummaryResponse"
              examples:
                RecentWindow:
                  summary: 최근 24h에 평가된 run 요약
                  value:
                    window: 24h
                    generated_at: "2026-09-01T09:30:00+00:00"
                    availability: available
                    total_runs: 7
                    evaluated_runs: 5
                    pass_runs: 3
                    warn_runs: 2
                    fail_runs: 1
        "400":
          description: 지원하지 않는 window 값
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /query:
    post:
      operationId: queryBuiltDataset
      summary: 서버가 resolve한 Silver/Gold table에 read-only SQL 실행
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryRequest"
            examples:
              AveragePm10ByStation:
                summary: 서버가 resolve한 Silver table 집계
                value:
                  dataset_id: seoul-air-quality
                  run_id: air-quality-20260815
                  stage: silver
                  source: air
                  sql: SELECT station_name, AVG(pm10_value) AS avg_pm10 FROM dataset GROUP BY station_name ORDER BY avg_pm10 DESC
                  limit: 100
      responses:
        "200":
          description: bounded query result preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryResponse"
              examples:
                AveragePm10Result:
                  summary: bounded row 결과와 현재 1.9.0 실행 시간 필드
                  value:
                    columns: [station_name, avg_pm10]
                    rows:
                      - station_name: 종로구
                        avg_pm10: 31.5
                      - station_name: 중구
                        avg_pm10: 28.0
                    truncated: false
                    execution_ms: 7
                    startup_ms: 12
                    engine_execution_ms: 5
        "400":
          description: invalid context, unsafe SQL, syntax/execution failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                UnsafeQuery:
                  summary: write statement가 포함된 SQL 거부
                  value:
                    error: only read-only SELECT queries are allowed
                    code: unsafe_query
                InvalidContext:
                  summary: dataset_id와 run snapshot이 일치하지 않음
                  value:
                    error: dataset does not match run snapshot
                    code: invalid_context
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: query context 소유권 위반
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                Forbidden:
                  summary: 다른 principal 소유 run
                  value:
                    error: forbidden
                    code: forbidden
        "404":
          description: stage artifact unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                ArtifactUnavailable:
                  summary: 요청 stage의 table artifact가 없음
                  value:
                    error: query artifact unavailable
                    code: artifact_unavailable
        "429":
          description: query 전용 capacity 포화. child process는 생성되지 않는다.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                QueryBusy:
                  summary: query 전용 동시 실행 capacity 포화
                  value:
                    error: query is busy
                    code: query_busy
        "504":
          description: query process timeout 및 강제 종료
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                QueryTimeout:
                  summary: 격리된 query process 제한 시간 초과
                  value:
                    error: query timed out
                    code: query_timeout

  /monitoring/summary:
    get:
      operationId: getMonitoringSummary
      summary: Builder API/Queue/Worker/Artifact Store 시스템 상태 요약 (#516)
      description: >-
        시스템 aggregate만 반환하며 개별 run의 dataset/owner/credential 정보는
        포함하지 않는다. Async build queue/worker(#511/#513)는 이 서비스가 항상
        생성하는 AsyncBuildExecutor의 read-only snapshot을 반영하므로 항상
        `availability: available`이다(#516). Provider 상태는 네트워크 프로브를
        유발하므로 이번 버전에서는 포함하지 않는다.
      responses:
        "200":
          description: 시스템 상태 요약
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoringSummaryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /monitoring/builds:
    get:
      operationId: getMonitoringBuilds
      summary: 시간대별 build 통계와 recent runs (#516)
      description: >-
        BuildIndex(ADR 0003) 기반 파생 집계. timezone은 UTC이며 bucket 경계는
        `[start, end)` 반열린이다. bucket 기준 timestamp는 `finished_at`이다.
        ENFORCE_OWNERSHIP+oidc principal일 때는 본인이 접근 가능한 run만
        집계·노출한다(#505).
      parameters:
        - name: window
          in: query
          required: false
          description: "집계 기간. 현재 24h만 지원 (기본값: 24h)"
          schema:
            type: string
            enum: [24h]
            default: 24h
        - name: bucket
          in: query
          required: false
          description: "집계 단위. 현재 hour만 지원 (기본값: hour)"
          schema:
            type: string
            enum: [hour]
            default: hour
      responses:
        "200":
          description: build 통계와 recent runs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoringBuildsResponse"
        "400":
          description: 지원하지 않는 window/bucket 값
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: >-
        서버에 KPUBDATA_BUILDER_API_KEY가 설정된 경우 필수. 미설정 시(로컬 개발 기본값)
        인증 없이 모든 요청이 허용된다 (#248).
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >-
        Google OIDC ID 토큰 (ADR 0009). Authorization: Bearer <jwt>.
        OIDC_ISSUER 미설정 시 비활성 — 기존 ApiKeyAuth 배포에 영향 없음 (#385).

  responses:
    Unauthorized:
      description: X-API-Key가 누락되었거나 서버에 설정된 키와 일치하지 않음
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          examples:
            MissingOrInvalidApiKey:
              summary: 인증 정보가 없거나 일치하지 않음
              value:
                error: unauthorized

  parameters:
    RunId:
      name: run_id
      in: path
      required: true
      description: build 실행 식별자(경로 안전한 단일 세그먼트)
      schema:
        type: string

    DatasetId:
      name: dataset_id
      in: path
      required: true
      description: >-
        BuildSpec.dataset_id 값. slash/space 등 경로에 그대로 쓸 수 없는 문자를
        포함할 수 있으므로 클라이언트는 percent-encoding해야 한다(#488).
      schema:
        type: string

    Stage:
      name: stage
      in: path
      required: true
      description: 조회할 medallion stage
      schema:
        type: string
        enum: [bronze, silver, gold]

    ProviderName:
      name: provider
      in: path
      required: true
      description: kpubdata runtime catalog에 등록된 Provider 식별자
      schema:
        type: string
        pattern: "^[a-z0-9][a-z0-9_-]{0,63}$"

    UploadId:
      name: upload_id
      in: path
      required: true
      description: >-
        POST /uploads가 발급한 불투명한 식별자(#498). 사용자가 지정한 값이 아니다.
      schema:
        type: string
        pattern: "^upl_[a-f0-9]{32}$"

  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: string
          description: 사람이 읽는 오류 요약 메시지
        code:
          type: string
          description: 클라이언트 분기용 안정적인 오류 코드(새 endpoint에서 사용)

    QueryRequest:
      type: object
      additionalProperties: false
      required: [dataset_id, run_id, stage, sql]
      properties:
        dataset_id: {type: string}
        run_id: {type: string}
        stage: {type: string, enum: [silver, gold]}
        source: {type: string}
        sql: {type: string, maxLength: 65536}
        limit: {type: integer, minimum: 1, maximum: 500, default: 100}

    QueryResponse:
      type: object
      additionalProperties: false
      required: [columns, rows, truncated, execution_ms, startup_ms, engine_execution_ms]
      properties:
        columns:
          type: array
          items: {type: string}
        rows:
          type: array
          items:
            type: object
            additionalProperties:
              $ref: "#/components/schemas/JsonValue"
        truncated: {type: boolean}
        execution_ms:
          type: integer
          minimum: 0
          description: >-
            QueryEngine.execute 진입부터 child payload 수신·검증과 child join까지의
            parent end-to-end monotonic wall time(ms). 기존 필드 의미를 유지한다.
        startup_ms:
          type: integer
          minimum: 0
          description: >-
            Parent의 QueryEngine.execute 진입부터 spawned child가 Polars import와 bounded
            SQL setup을 끝내고 scan_parquet를 시작하기 직전까지의 monotonic time(ms).
        engine_execution_ms:
          type: integer
          minimum: 0
          description: >-
            Child에서 scan_parquet 직전부터 SQLContext 생성, execute, collect 완료
            직후까지의 Polars engine monotonic time(ms).

    ValidationError:
      type: object
      description: >-
        validate/build/preview의 spec 검증 실패 응답. SpecLoadError는
        {status:"error", error}, ValidationError는 {status:"invalid", problems}로
        나뉜다.
      required: [status]
      properties:
        status:
          type: string
          enum: [error, invalid]
        error:
          type: string
          description: status=error일 때의 파싱 오류 메시지
        problems:
          type: array
          description: status=invalid일 때의 검증 문제 목록
          items:
            type: string

    JsonValue:
      description: 표준 JSON으로 직렬화 가능한 값(null, scalar, array, object)
      type: [string, number, boolean, object, array, "null"]
      additionalProperties:
        $ref: "#/components/schemas/JsonValue"
      items:
        $ref: "#/components/schemas/JsonValue"

    BuildSpec:
      type: object
      description: >-
        선언적 빌드 명세(snake_case). dataset_id/title/description/sources/exports는
        필수이고 나머지 필드는 선택이다. 세부 스키마는 spec 모델
        (spec/models.py)을 따른다. 제거된 transforms/normalization_mode 필드는
        허용하지 않는다.
      additionalProperties: true
      allOf:
        - not:
            required: [transforms]
        - not:
            required: [normalization_mode]
      required: [dataset_id, title, description, sources, exports]
      properties:
        dataset_id:
          type: string
        title:
          type: string
        description:
          type: string
        sources:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/SourceRef"
        exports:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ExportTarget"
        metadata:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/JsonValue"
        publish:
          type: boolean
          default: false
        splits:
          oneOf:
            - $ref: "#/components/schemas/SplitSpec"
            - type: "null"
        pii:
          oneOf:
            - $ref: "#/components/schemas/PiiPolicy"
            - type: "null"
        license:
          type: [string, "null"]
        quality:
          oneOf:
            - $ref: "#/components/schemas/QualityPolicy"
            - type: "null"
        composition:
          description: >-
            두 source를 join해 하나의 결합 Gold dataset을 추가로 만드는 계약
            (#506). None이면 기존과 동일하게 source별 독립 Gold만 생성한다.
          oneOf:
            - $ref: "#/components/schemas/CompositionSpec"
            - type: "null"

    SourceRef:
      type: object
      description: >-
        Canonical source 참조(#498). `kind`로 public_api(기본, 하위 호환)/file/url을
        구분한다. `kind`를 생략한 source는 항상 `public_api`로 해석되며
        provider/dataset이 필수다. `file`은 upload_id+format(+encoding)이,
        `url`은 endpoint(+method+format)가 필수다 — kind별 조건부 필수/금지
        조합은 OpenAPI object schema로 표현하지 않고(이 계약의 기존 패턴과 동일하게
        `additionalProperties`로 열어두고) `spec/loader.py`·`validator.py`가
        엄격히 강제한다. url source는 P0 범위(GET, Auth=None)의 SSRF-safe fetch만
        허용하며 https가 아닌 scheme·userinfo가 포함된 endpoint는 거부된다.
        제거된 normalization_mode 필드는 허용하지 않는다.
      additionalProperties: true
      not:
        required: [normalization_mode]
      properties:
        kind:
          type: string
          enum: [public_api, file, url]
          default: public_api
        provider:
          type: string
          description: kind="public_api"에서 필수.
        dataset:
          type: string
          description: kind="public_api"에서 필수.
        params:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/JsonValue"
        alias:
          type: string
          default: ""
        schema:
          oneOf:
            - $ref: "#/components/schemas/SchemaContract"
            - type: "null"
        upload_id:
          type: string
          pattern: "^upl_[a-f0-9]{32}$"
          description: kind="file"에서 필수. POST /uploads가 발급한 식별자.
        format:
          type: string
          enum: [csv, json, jsonl, parquet]
          description: >-
            kind="file"에서 필수(업로드 시 검증된 format과 정확히 일치해야 함).
            kind="url"에서는 선택(json/jsonl/csv만 허용, 생략 시 응답
            Content-Type로 추론).
        encoding:
          type: string
          default: utf-8
          description: kind="file"에서 텍스트(csv/json/jsonl) 디코딩에 쓰는 인코딩.
        endpoint:
          type: string
          format: uri
          description: kind="url"에서 필수. https만 허용한다(SSRF 정책, #498).
        method:
          type: string
          enum: [GET]
          default: GET
          description: kind="url"에서만 쓰이며 P0는 GET만 허용한다.

    SchemaContract:
      type: object
      description: Silver 정규화 및 required-column/dtype 검증 계약
      properties:
        required:
          type: array
          items:
            type: string
          default: []
        dtypes:
          type: object
          additionalProperties:
            type: string
          default: {}
        casts:
          type: object
          additionalProperties:
            type: string
          default: {}

    UploadMetadata:
      type: object
      description: >-
        안전한(secret-free) 업로드 메타데이터(#498). content는 절대 포함하지
        않으며, 이 API로는 원문 content를 다시 조회할 수 없다.
      required: [upload_id, format, encoding, size_bytes, original_filename, created_at]
      properties:
        upload_id:
          type: string
          pattern: "^upl_[a-f0-9]{32}$"
        format:
          type: string
          enum: [csv, json, jsonl, parquet]
        encoding:
          type: string
        size_bytes:
          type: integer
          minimum: 0
        original_filename:
          type: [string, "null"]
          description: 표시 전용 원본 파일명(파일시스템 경로 아님).
        created_at:
          type: string
          format: date-time

    UploadDeleteResponse:
      type: object
      required: [upload_id, deleted]
      properties:
        upload_id:
          type: string
        deleted:
          type: boolean

    ExportTarget:
      type: object
      required: [kind, output_path]
      properties:
        kind:
          type: string
        output_path:
          type: string
        options:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/JsonValue"

    SplitSpec:
      type: object
      required: [mode]
      properties:
        mode:
          type: string
          enum: [ratio, key]
        ratios:
          type: object
          additionalProperties:
            type: number
          default: {}
        key:
          type: string
          default: ""
        seed:
          type: integer
          default: 0

    PiiPolicy:
      type: object
      properties:
        mode:
          type: string
          enum: [block, warn, allow]
          default: block
        allow_columns:
          type: array
          items:
            type: string
          default: []

    QualitySeverity:
      type: string
      description: >-
        quality rule 위반 시 심각도 (#486). "warn"(기본, Build 계속 진행) |
        "fail"(Gold 진입 전 소스 실패).
      enum: [warn, fail]
      default: warn

    QualityRangeRule:
      type: object
      description: >-
        숫자 컬럼의 최소/최대 범위 규칙 (#486). min/max는 포함(inclusive) 경계이며
        자유형 eval을 쓰지 않는 typed rule이다. null 값은 위반으로 계산하지 않는다.
      required: [column]
      properties:
        column:
          type: string
        min:
          type: [number, "null"]
        max:
          type: [number, "null"]
        severity:
          $ref: "#/components/schemas/QualitySeverity"

    QualityCompareColumnsRule:
      type: object
      description: >-
        두 컬럼 간 비교 규칙 (#486). operator는 제한된 어휘만 허용하며 자유형
        expression/eval을 쓰지 않는다. 두 컬럼 모두 null이 아닌 행만 평가한다.
      required: [left, operator, right]
      properties:
        left:
          type: string
        operator:
          type: string
          enum: [eq, ne, gt, gte, lt, lte]
        right:
          type: string
        severity:
          $ref: "#/components/schemas/QualitySeverity"

    QualityPolicy:
      type: object
      description: >-
        데이터 품질 임계 정책. max_duplicate_rate/max_null_ratio/min_rows(#446)는
        기존 syntax와 100% 하위 호환이며 기본 severity는 "warn"이다. 대응하는
        ``*_severity`` 필드(#486)로 명시적 FAIL을 선언할 수 있다.
      properties:
        max_duplicate_rate:
          type: [number, "null"]
        max_duplicate_rate_severity:
          $ref: "#/components/schemas/QualitySeverity"
        max_null_ratio:
          type: object
          additionalProperties:
            type: number
          default: {}
        max_null_ratio_severity:
          type: object
          description: 컬럼별 max_null_ratio severity override. 선언 안 된 컬럼은 "warn".
          additionalProperties:
            $ref: "#/components/schemas/QualitySeverity"
          default: {}
        min_rows:
          type: [integer, "null"]
        min_rows_severity:
          $ref: "#/components/schemas/QualitySeverity"
        range:
          type: array
          items:
            $ref: "#/components/schemas/QualityRangeRule"
          default: []
        compare_columns:
          type: array
          items:
            $ref: "#/components/schemas/QualityCompareColumnsRule"
          default: []

    JoinSpec:
      type: object
      description: >-
        두 source를 결합하는 equi-join 계약(#506). 초기 범위는 두 source
        단일 join으로 제한한다 — 3개 이상 join graph, 자유형 SQL join
        조건은 지원하지 않는다. left/right는 sources[].alias를 참조해야
        하며, 참조되는 source는 alias를 반드시 선언해야 한다.
      required: [left, right, left_key, right_key]
      properties:
        left:
          type: string
          description: 왼쪽 source의 alias (sources[].alias 참조)
        right:
          type: string
          description: 오른쪽 source의 alias
        left_key:
          type: string
          description: 왼쪽 테이블의 join key 컬럼명
        right_key:
          type: string
          description: 오른쪽 테이블의 join key 컬럼명
        type:
          type: string
          enum: [inner, left]
          default: inner
        on_duplicate_key:
          type: string
          enum: [warn, fail]
          default: warn
          description: >-
            양쪽 join key가 모두 중복 값을 가져(many-to-many) 결과 행이
            곱셈으로 폭증할 수 있을 때의 처리. warn은 경고만 남기고 결과를
            만들고, fail은 빌드를 실패시킨다.

    CompositionSpec:
      type: object
      description: 여러 source를 하나의 결합 Gold dataset으로 조립하는 계약(#506).
      required: [name, join]
      properties:
        name:
          type: string
          description: >-
            결합 결과 Gold dataset 이름. 다른 source의 output key(alias 또는
            provider.dataset)와 겹치면 안 된다.
        join:
          $ref: "#/components/schemas/JoinSpec"

    SpecRequest:
      type: object
      required: [spec]
      properties:
        spec:
          type: string
          description: BuildSpec을 YAML로 직렬화한 문자열

    PreviewRequest:
      type: object
      required: [spec]
      properties:
        spec:
          type: string
          description: BuildSpec을 YAML로 직렬화한 문자열
        limit:
          type: integer
          minimum: 1
          maximum: 1000
          default: 5
          description: >-
            상한 1000은 #497에서 새로 도입됐다(behavioral tightening — v1.8.0
            이전에는 상한이 없었다). 1000을 넘는 값은 400을 반환한다.
        sample_mode:
          type: string
          enum: [first, random]
          default: first
          description: >-
            "first"는 상위 N행, "random"은 seed 기반 결정적 무작위 N행이다(#497).
            Source sample과 Silver sample은 항상 같은 행 인덱스를 공유한다 —
            생략하면 기존 client와 동일하게 상위 N행을 반환한다.
        seed:
          type: integer
          description: >-
            sample_mode=random일 때 쓰는 결정적 시드(#497). 생략하면 고정
            기본값을 써서 seed 없이도 재현 가능하다. sample_mode=first에서는
            무시된다.

    BuildRequest:
      type: object
      required: [spec]
      properties:
        spec:
          type: string
          description: BuildSpec을 YAML로 직렬화한 문자열
        run_id:
          type: string
          description: 명시 시 비어있지 않은 경로 안전한 단일 세그먼트여야 한다

    VersionResponse:
      type: object
      required: [service, api_version]
      properties:
        service:
          type: string
        api_version:
          type: string

    HealthResponse:
      type: object
      required: [status]
      description: liveness probe 응답 (버전·메타 정보 없이 status만, #372)
      properties:
        status:
          type: string
          const: "ok"

    CatalogResponse:
      type: object
      required: [providers]
      description: provider/dataset 카탈로그 (#416, BL2)
      properties:
        providers:
          type: array
          items:
            $ref: "#/components/schemas/CatalogProvider"

    CatalogProvider:
      type: object
      required: [name, datasets]
      properties:
        name:
          type: string
        datasets:
          type: array
          items:
            $ref: "#/components/schemas/CatalogDataset"

    CatalogDataset:
      type: object
      required:
        [
          name,
          title,
          description,
          tags,
          source_url,
          representation,
          operations,
          query_support,
          requires_service_key,
          request_parameters,
          application,
        ]
      description: >
        DatasetRef의 public/canonical metadata를 allowlist로 직렬화한다 (#490).
        raw_metadata는 provider 내부 정보/secret이 섞일 수 있어 노출하지 않는다.
        metadata가 없는 dataset은 description/source_url/query_support가 null,
        tags/operations가 빈 배열이다.
      properties:
        name:
          type: string
        title:
          type: string
        description:
          type: [string, "null"]
          description: 사람이 읽는 데이터셋 설명 (없으면 null)
        tags:
          type: array
          items:
            type: string
          description: 탐색용 분류 태그 (정렬된 문자열 배열, 없으면 빈 배열)
        source_url:
          type: [string, "null"]
          description: 원본 API 문서/데이터 포털 URL (없으면 null)
        representation:
          type: string
          enum: [api_json, api_xml, file_csv, file_excel, sheet, other]
        operations:
          type: array
          items:
            type: string
            enum: [list, get, schema, raw, download]
          description: 지원 작업 (정렬된 문자열 배열, 없으면 빈 배열)
        query_support:
          oneOf:
            - type: "null"
            - $ref: "#/components/schemas/CatalogQuerySupport"
          description: 목록 질의 capability (알려진 경우만, 없으면 null)
        requires_service_key:
          type: boolean
        request_parameters:
          type: array
          items:
            $ref: "#/components/schemas/CatalogRequestParameter"
          description: >
            선택한 Dataset에 전달하는 요청 파라미터의 secret-free 설명 (#490 후속,
            additive). raw_metadata에서 allowlist로 추린 것으로 serviceKey 등 시크릿
            파라미터는 포함되지 않는다. metadata가 없으면 빈 배열.
        application:
          oneOf:
            - type: "null"
            - $ref: "#/components/schemas/CatalogApplication"
          description: >
            공공데이터포털처럼 API Key 발급과 Dataset별 활용신청이 별개일 수 있는
            경우의 안내 (#490 후속, additive). raw_metadata.application에서
            그대로 전달한 것으로, 없으면 null(활용신청이 필요 없다는 뜻이 아니라
            알려진 바 없음). Studio는 신청 완료/승인 여부를 이 필드로 추측하지 않는다
            — Preview 성공이 실제 사용 가능 여부의 최종 확인이다.

    CatalogRequestParameter:
      type: object
      required: [name, required, description, example]
      properties:
        name:
          type: string
          description: 요청 파라미터 이름 (시크릿/serviceKey 계열은 제외됨)
        required:
          type: boolean
          description: 이 파라미터가 필수인지
        description:
          type: [string, "null"]
          description: 사람이 읽는 설명 (없으면 null)
        example:
          type: [string, "null"]
          description: 근거 있는 예시 값 (없으면 null)

    CatalogApplication:
      type: object
      required: [required, url]
      description: >
        Dataset별 활용신청 안내 (API Key 발급과 별개일 수 있음). Studio는 신청
        완료/승인 상태를 이 필드로 만들어내지 않는다 — 알 수 없으면 알 수 없는
        상태로 남긴다.
      properties:
        required:
          type: boolean
          description: 이 Dataset이 API Key 등록과 별도로 활용신청이 필요할 수 있는지
        url:
          type: string
          format: uri
          description: 공식 Dataset 상세/활용신청 페이지 (http/https만 허용)

    CatalogQuerySupport:
      type: object
      required: [pagination, filterable_fields, sortable_fields, time_range, max_page_size]
      properties:
        pagination:
          type: string
          enum: [offset, index, cursor, none]
        filterable_fields:
          type: array
          items:
            type: string
        sortable_fields:
          type: array
          items:
            type: string
        time_range:
          type: boolean
        max_page_size:
          type: [integer, "null"]
          minimum: 1

    ProviderListResponse:
      type: object
      required: [providers]
      additionalProperties: false
      properties:
        providers:
          type: array
          items:
            $ref: "#/components/schemas/ProviderSummary"

    ProviderSummary:
      type: object
      required: [provider, requires_credential, configured]
      additionalProperties: false
      properties:
        provider:
          type: string
        requires_credential:
          type: boolean
        configured:
          type: boolean
          description: user credential, server default, 또는 무인증 Provider이면 true

    ProviderCredentialPutRequest:
      type: object
      required: [credential]
      additionalProperties: false
      properties:
        credential:
          type: string
          minLength: 1
          writeOnly: true
          description: 저장할 raw credential. 응답, 로그, manifest, event에 포함하지 않는다.

    ProviderCredentialMetadata:
      type: object
      required: [provider, configured, masked, updated_at]
      additionalProperties: false
      description: owner_id와 credential 원문을 포함하지 않는 사용자별 저장 메타데이터
      properties:
        provider:
          type: string
        configured:
          type: boolean
        masked:
          type: [string, "null"]
          description: 설정 시 고정 마스크. credential 일부도 노출하지 않는다.
        updated_at:
          type: [string, "null"]
          format: date-time

    ProviderCredentialGetResponse:
      type: object
      required: [configured, masked, updated_at]
      additionalProperties: false
      description: GET credential 응답. provider, owner_id, credential 원문을 포함하지 않는다.
      properties:
        configured:
          type: boolean
        masked:
          type: [string, "null"]
          description: 설정 시 고정 마스크. credential 일부도 노출하지 않는다.
        updated_at:
          type: [string, "null"]
          format: date-time

    ProviderTestResponse:
      type: object
      required: [provider, status, configured, latency_ms, checked_at]
      additionalProperties: false
      properties:
        provider:
          type: string
        status:
          type: string
          enum: [connected, failed, not_configured]
        configured:
          type: boolean
        latency_ms:
          type: integer
          minimum: 0
        checked_at:
          type: string
          format: date-time
        error_category:
          type: string
          enum: [auth, network, timeout, provider, unknown]
        response_code:
          type: integer
          minimum: 100
          maximum: 599
          description: kpubdata가 구조적으로 신뢰 가능한 status를 제공한 경우에만 포함

    StructuredProblem:
      type: object
      required: [code, path, message]
      description: 구조화된 검증 문제 (#417, BL3)
      properties:
        code:
          type: string
        path:
          type: string
        message:
          type: string
        hint:
          type: string
          nullable: true

    ValidateResponse:
      type: object
      required: [status, dataset_id, api_version]
      properties:
        status:
          type: string
          enum: [valid]
        dataset_id:
          type: string
        api_version:
          type: string

    PreviewResponse:
      type: object
      required: [dataset_id, previews]
      properties:
        dataset_id:
          type: string
        previews:
          type: array
          items:
            $ref: "#/components/schemas/SourcePreview"

    SourcePreview:
      type: object
      required:
        - source_key
        - status
        - error
        - schema
        - sample
        - total_rows
        - statistics
        - quality_results
        - source_sample
        - sample_mode
        - diff_available
        - diffs
        - transform_summary
        - diff_truncated
      properties:
        source_key:
          type: string
        status:
          type: string
          enum: [ok, failed]
        error:
          type: [string, "null"]
        schema:
          type: array
          items:
            type: object
            required: [name, dtype, nullable, unique_count]
            properties:
              name:
                type: string
              dtype:
                type: string
              nullable:
                type: boolean
              unique_count:
                type: integer
        sample:
          type: array
          items:
            type: object
            additionalProperties:
              $ref: "#/components/schemas/JsonValue"
        total_rows:
          type: integer
        statistics:
          $ref: "#/components/schemas/TableStatistics"
        quality_results:
          type: array
          description: >-
            구조화된 Quality/Schema 평가 결과 (#486). Build와 동일한 evaluator
            결과이며 PASS를 포함한 실제로 평가된 check만 담는다. drift는 preview가
            워크스페이스에 아무것도 쓰지 않으므로 포함하지 않는다.
          items:
            $ref: "#/components/schemas/QualityCheckResult"
        source_sample:
          type: array
          description: >-
            변환 전 Bronze 원본 sample(#497). diff_available=false여도 최선
            노력으로 채워질 수 있다(조회 자체가 실패하면 빈 배열).
          items:
            type: object
            additionalProperties:
              $ref: "#/components/schemas/JsonValue"
        sample_mode:
          type: string
          enum: [first, random]
          description: 이 응답에 실제로 적용된 sampling 방식.
        diff_available:
          type: boolean
          description: >-
            source_sample[i]와 sample[i]가 동일 논리 행을 가리킨다고 보장할 수
            있을 때만 true다(#497). 행 수 불일치, row reorder/filter 가능성,
            조회 실패 등에서는 false이며 diffs/transform_summary는 빈 값이다.
            잘못된 index 기반 비교보다 false가 우선이다.
        diffs:
          type: array
          description: >-
            diff_available=true일 때만 채워지는 셀 단위 변경 목록(#497). limit은
            행 수만 제한하고 컬럼 수는 제한하지 않으므로, 실제 변경 셀 수가
            많으면 최대 1000개(MAX_PREVIEW_DIFF_ITEMS)까지만 담긴다 —
            diff_truncated로 잘렸는지 확인한다.
          items:
            $ref: "#/components/schemas/PreviewDiffItem"
        transform_summary:
          description: >-
            diff_available=true일 때만 채워지는 변경 요약(#497). 비교 가능한
            sample 범위 기준이며 전체 dataset 변화량이 아니다. diffs가
            diff_truncated로 잘려도 changed_cells/changed_rows는 잘리지 않은
            실제 합계를 유지한다.
          oneOf:
            - $ref: "#/components/schemas/PreviewTransformSummary"
            - type: "null"
        diff_truncated:
          type: boolean
          description: >-
            diffs가 MAX_PREVIEW_DIFF_ITEMS(1000) 상한에 걸려 실제 변경 셀 전부를
            담지 못했으면 true다(#497 sample/diff memory 상한). diffs를 전체
            diff로 오인하지 않도록 명시한다. diff_available=false면 애초에
            diff를 시도하지 않았으므로 항상 false다.

    PreviewDiffItem:
      type: object
      description: >-
        Source↔Silver 셀 단위 변경 하나(#497). diff_available=true인 source의
        diffs에만 등장한다.
      additionalProperties: false
      required: [row, column, before, after, transform]
      properties:
        row:
          type: integer
          minimum: 0
          description: source_sample/sample 배열 내 0-based 위치(전체 dataset의 절대 행 번호 아님).
        column:
          type: string
        before:
          $ref: "#/components/schemas/JsonValue"
        after:
          $ref: "#/components/schemas/JsonValue"
        transform:
          type: [string, "null"]
          description: >-
            컬럼에 선언된 캐스팅(schema.casts)이 있을 때만 "cast:{dtype}" 형태로
            채운다. 알 수 없는 변환은 추측하지 않고 null.

    PreviewTransformSummary:
      type: object
      description: 비교 가능한 sample 범위에서 계산한 변경 요약(#497).
      additionalProperties: false
      required: [changed_cells, changed_rows]
      properties:
        changed_cells:
          type: integer
          minimum: 0
        changed_rows:
          type: integer
          minimum: 0

    QualityCheckResult:
      type: object
      description: 단일 quality/schema check의 구조화된 결과 (#486).
      additionalProperties: false
      required:
        - source_key
        - category
        - rule
        - column
        - status
        - actual
        - threshold
        - affected_rows
        - evaluated_rows
        - detail
      properties:
        source_key:
          type: string
        category:
          type: string
          description: >-
            check 대분류(예: duplicate, missing, row_count, schema, range,
            compare_columns).
        rule:
          type: string
          description: >-
            구체적 rule 이름(예: max_duplicate_rate, max_null_ratio, min_rows,
            required_column, dtype, range, compare_columns).
        column:
          type: [string, "null"]
          description: >-
            관련 컬럼명. 테이블 전체 규칙이면 null. compare_columns는
            "{left},{right}" 형식의 합성값을 쓴다.
        status:
          type: string
          enum: [pass, warn, fail]
        actual:
          $ref: "#/components/schemas/JsonValue"
        threshold:
          $ref: "#/components/schemas/JsonValue"
          description: >-
            원래 규칙의 비교 조건. 기존 비율/행 수 규칙은 numeric 값을 유지한다.
            range는 {min, max}, compare_columns는 {operator, right_column} 구조다.
        affected_rows:
          type: [integer, "null"]
          description: 위반 행 수. 정확히 셀 수 없으면 null(임의 추정 금지).
        evaluated_rows:
          type: [integer, "null"]
          description: 실제 평가에 사용된 행 수. 의미 없으면 null.
        detail:
          type: [string, "null"]
          description: >-
            평가 불가 이유 등 사용자가 이해할 수 있는 결정적 설명. 정상 평가 결과는
            null이며 내부 예외·스택 트레이스·절대 경로를 포함하지 않는다.

    SchemaDriftFinding:
      type: object
      description: >-
        구조화된 schema drift 관찰 (#445, #486). deterministic 감지 결과이며
        PASS/WARN/FAIL 게이트에는 관여하지 않는다(참고용).
      additionalProperties: false
      required: [kind, column, detail]
      properties:
        kind:
          type: string
          enum: [column_added, column_removed, dtype_changed, row_count_jump]
        column:
          type: [string, "null"]
        detail:
          type: string

    BuildQualityResponse:
      type: object
      description: >-
        run의 구조화된 quality_results/schema_drift 조회 응답 (#486). `availability`와
        `evaluated_checks`는 빈 `quality_results`가 "0건 평가"인지 "애초에 계산된 적
        없음"(legacy/partial run)인지 구분한다(#514).
      required: [run_id, availability, evaluated_checks, quality_results, schema_drift]
      properties:
        run_id:
          type: string
        availability:
          type: string
          enum: [available, partial, unavailable]
          description: >-
            available: 이 run이 시도한 모든 source의 quality 결과가 있다(0건일 수
            있음). partial: 일부 source만 있다(예: 한 source의 Silver 실패).
            unavailable: 결과가 전혀 없다 — quality_results 필드 자체가 없는 legacy
            run(#486 이전)뿐 아니라, 필드는 있지만(`{}`) 시도한 source 중 하나도
            결과가 없는 새 run도 포함한다(예: 모든 source가 quality 단계 진입 전에
            실패).
        evaluated_checks:
          type: integer
          minimum: 0
          description: quality_results 전체에서 실제로 평가된 check(PASS/WARN/FAIL) 총합.
        quality_results:
          type: object
          description: source_key → QualityCheckResult 목록.
          additionalProperties:
            type: array
            items:
              $ref: "#/components/schemas/QualityCheckResult"
        schema_drift:
          type: object
          description: source_key → SchemaDriftFinding 목록.
          additionalProperties:
            type: array
            items:
              $ref: "#/components/schemas/SchemaDriftFinding"

    DatasetQualityHistoryEntry:
      type: object
      description: >-
        단일 run의 quality PASS/WARN/FAIL 집계 (#486). evaluated_checks==0이면
        rule_pass_rate는 반드시 null이다(미평가를 100%로 위장하지 않는다).
      required:
        - run_id
        - timestamp
        - status
        - pass_count
        - warn_count
        - fail_count
        - evaluated_checks
        - rule_pass_rate
        - validated_rows
      properties:
        run_id:
          type: string
        timestamp:
          type: [string, "null"]
          description: finished_at(없으면 started_at).
        status:
          type: string
          enum: [ok, failed, cancelled]
        pass_count:
          type: integer
        warn_count:
          type: integer
        fail_count:
          type: integer
        evaluated_checks:
          type: integer
          description: pass_count + warn_count + fail_count.
        rule_pass_rate:
          type: [number, "null"]
          description: pass_count / evaluated_checks. evaluated_checks==0이면 null.
        validated_rows:
          type: [integer, "null"]
          description: >-
            run/source의 Silver row_count 합계(#488 row_counts semantics 재사용).
            evaluated_rows를 rule 수만큼 중복 합산하지 않는다.

    DatasetQualityHistoryResponse:
      type: object
      required: [dataset_id, runs]
      properties:
        dataset_id:
          type: string
        runs:
          type: array
          description: newest first.
          items:
            $ref: "#/components/schemas/DatasetQualityHistoryEntry"

    QualitySummaryResponse:
      type: object
      description: >-
        최근 window cross-run quality aggregate (#486 후속). 시간 기준은 canonical
        run timestamp(finished_at, 없으면 started_at)를 UTC로 파싱한
        `(generated_at - window, generated_at]`이며, 파싱 불가능한 timestamp를 가진
        run은 창에 놓을 수 없어 제외된다. 미평가/unavailable run을 PASS로 세지
        않는다.
      required:
        - window
        - generated_at
        - availability
        - total_runs
        - evaluated_runs
        - pass_runs
        - warn_runs
        - fail_runs
      additionalProperties: false
      properties:
        window:
          type: string
          enum: [24h]
        generated_at:
          type: string
          description: 집계 기준 시각(UTC, ISO 8601, 초 단위).
        availability:
          type: string
          enum: [available, unavailable]
          description: >-
            available: 집계 성공(run 0건이어도 유효). unavailable: 접근 가능한 run
            목록 자체를 열거하지 못함 — 이때 모든 count는 0이며 "0건"과 구분된다.
        total_runs:
          type: integer
          minimum: 0
          description: window 안의 접근 가능한 canonical run 수(status 무관).
        evaluated_runs:
          type: integer
          minimum: 0
          description: >-
            그 중 structured quality가 실제로 평가된(evaluated_checks > 0) run 수.
        pass_runs:
          type: integer
          minimum: 0
          description: evaluated run 중 WARN·FAIL 결과가 하나도 없는 run 수.
        warn_runs:
          type: integer
          minimum: 0
          description: >-
            evaluated run 중 WARN 결과가 하나 이상인 run 수(같은 run의 WARN check
            여러 개여도 1). Studio Home "QUALITY WARN (24H)" KPI. fail_runs와
            독립적이라 겹칠 수 있다.
        fail_runs:
          type: integer
          minimum: 0
          description: >-
            evaluated run 중 FAIL 결과가 하나 이상인 run 수. `pass_runs +
            (WARN 또는 FAIL이 있는 run 수) == evaluated_runs`.

    TableStatistics:
      type: object
      required: [row_count, null_counts, duplicate_rate]
      properties:
        row_count:
          type: integer
        null_counts:
          type: object
          additionalProperties:
            type: integer
        duplicate_rate:
          type: number

    BuildOutcome:
      type: object
      required: [source_key, status, stages_completed, error]
      properties:
        source_key:
          type: string
        status:
          type: string
          enum: [ok, failed]
        stages_completed:
          type: array
          items:
            type: string
        error:
          type: [string, "null"]

    BuildSuccessResponse:
      type: object
      required: [status, run_id, outcomes, manifest, api_version]
      properties:
        status:
          type: string
          enum: [ok]
        run_id:
          type: string
        outcomes:
          type: array
          items:
            $ref: "#/components/schemas/BuildOutcome"
        composition:
          description: BuildSpec.composition이 없으면 null (#506).
          oneOf:
            - $ref: "#/components/schemas/CompositionOutcome"
            - type: "null"
        manifest:
          type: string
          description: 기록된 manifest.json의 경로
        api_version:
          type: string

    BuildFailureResponse:
      type: object
      required: [status, run_id, outcomes, manifest, api_version, error]
      properties:
        status:
          type: string
          enum: [failed]
        run_id:
          type: string
        outcomes:
          type: array
          items:
            $ref: "#/components/schemas/BuildOutcome"
        composition:
          description: BuildSpec.composition이 없으면 null (#506).
          oneOf:
            - $ref: "#/components/schemas/CompositionOutcome"
            - type: "null"
        manifest:
          type: string
          description: partial 정책으로 남은 manifest.json의 경로
        api_version:
          type: string
        error:
          type: string
          description: >-
            첫 번째 실패 outcome의 error에서 파생된 human-readable 요약
            (#226). 모든 source가 성공하고 composition만 실패한 경우
            composition.error에서 파생된다 (#506).

    ArtifactsResponse:
      type: object
      required: [run_id, files]
      properties:
        run_id:
          type: string
        files:
          type: array
          items:
            type: string
            description: run 디렉터리 기준 상대 파일 경로

    BuildManifest:
      type: object
      required: [build_id, started_at, finished_at, schema_version]
      additionalProperties: true
      properties:
        build_id:
          type: string
        started_at:
          type: string
        finished_at:
          type: string
        schema_version:
          type: string
        status:
          type: string
          enum: [ok, failed, cancelled]
          description: >-
            run의 종단 상태 (#481, additive). `ok`/`failed`는 기존에 `errors`
            유무로 파생하던 값과 동일하고, `cancelled`는 협력적 취소로 종료된
            run이다. legacy manifest에는 이 필드가 없다 — reader는 부재 시
            기존대로 `errors` 유무에서 파생해야 한다. 취소된 run도 이미 실패한
            source의 사유를 `errors`에 그대로 보존한다(취소가 실패를 삼키지
            않는다).
        partial:
          type: boolean
          description: >-
            이 run이 정상 완료 전에 종료되어 `outputs`가 부분 산출물임을 뜻한다
            (#481, additive). 현재는 `status: cancelled`인 run에만 `true`이며,
            취소 시점까지 실제로 기록된 산출물만 담긴다 — 실행되지 않은 stage는
            성공으로 기록되지 않는다. 부분 산출물은 기본적으로 삭제하지
            않는다(감사/디버깅 목적 보존).
        inputs:
          type: array
          items:
            type: string
        outputs:
          type: array
          items:
            type: string
        warnings:
          type: array
          items:
            type: string
        errors:
          type: array
          items:
            type: string
        row_counts:
          type: object
          additionalProperties:
            type: integer
        inputs_fingerprint:
          type: [string, "null"]
        created_by:
          type: [string, "null"]
          description: >-
            빌드를 요청한 principal의 display/legacy 라벨(#388). 사람이 읽는
            표시용이며, ownership 판정에는 owner_id가 없는 legacy run에 한해
            하위 호환 폴백으로만 쓰인다.
        quality_results:
          type: object
          description: >-
            source_key → QualityCheckResult 목록 (#486, additive). legacy
            manifest는 이 필드가 없다 — 부재를 "미평가"로 해석해야 하며
            "전부 PASS"로 해석하면 안 된다.
          additionalProperties:
            type: array
            items:
              $ref: "#/components/schemas/QualityCheckResult"
        schema_drift:
          type: object
          description: source_key → SchemaDriftFinding 목록 (#486, additive).
          additionalProperties:
            type: array
            items:
              $ref: "#/components/schemas/SchemaDriftFinding"
        composition:
          description: >-
            BuildSpec.composition으로 두 source를 join한 결과의 출처 추적
            정보(#506, additive). composition이 없거나 실행되지 않았으면
            null이다 — legacy manifest reader는 부재와 null을 모두
            "composition 미사용 run"으로 해석해야 한다.
          oneOf:
            - $ref: "#/components/schemas/CompositionProvenance"
            - type: "null"

    CompositionProvenance:
      type: object
      description: composition(join) 실행 결과의 상세 출처 정보 (#506).
      required:
        - name
        - left
        - right
        - join_type
        - left_key
        - right_key
        - left_row_count
        - left_distinct_key_count
        - right_row_count
        - right_distinct_key_count
        - output_row_count
        - duplicate_key_warning
      properties:
        name:
          type: string
        left:
          type: string
        right:
          type: string
        join_type:
          type: string
          enum: [inner, left]
        left_key:
          type: string
        right_key:
          type: string
        left_row_count:
          type: integer
        left_distinct_key_count:
          type: integer
          description: 왼쪽 join key의 distinct 값 수 (null 제외)
        right_row_count:
          type: integer
        right_distinct_key_count:
          type: integer
          description: 오른쪽 join key의 distinct 값 수 (null 제외)
        output_row_count:
          type: integer
        duplicate_key_warning:
          type: boolean
          description: >-
            양쪽 join key가 모두 중복 값을 가져(many-to-many) 행 폭증 위험이
            감지됐는지 여부. 실제 처리(경고만/빌드 실패)는 JoinSpec의
            on_duplicate_key가 결정한다 — 이 필드는 감지 여부만 기록한다.

    CompositionOutcome:
      type: object
      description: >-
        composition(join) 실행 결과 (#506). source별 BuildOutcome과 별도로
        노출된다 — bronze/silver/gold stage 개념이 적용되지 않고, "combined
        result"로 명확히 구분되어야 하기 때문이다.
      required: [name, status, error]
      properties:
        name:
          type: string
        status:
          type: string
          enum: [ok, failed, skipped]
          description: >-
            ok(성공) / failed(join 자체 실행 실패: 키 부재·타입 불일치·명시적
            fail 게이트) / skipped(참조한 source가 실패해 join을 시도조차
            못함)
        error:
          type: [string, "null"]

    BuildSpecSnapshotResponse:
      type: object
      required: [run_id, spec, spec_digest]
      additionalProperties: false
      properties:
        run_id:
          type: string
        spec:
          type: string
          description: credential 값이 redaction된 canonical YAML
        spec_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
          description: >-
            실제 저장된 redaction 후 canonical buildspec.yaml bytes의 SHA-256.
            credential 값만 다른 spec은 같은 digest가 될 수 있다.

    BuildSummary:
      type: object
      required: [run_id, status]
      properties:
        run_id:
          type: string
          description: 빌드 실행 식별자
        status:
          type: string
          enum: [ok, failed, cancelled]
          description: >-
            빌드 상태. `cancelled`(#481)는 협력적 취소로 종료된 run이며 부분
            산출물과 partial manifest가 함께 남는다 — 성공한 빌드로 취급하면
            안 된다.
        started_at:
          type: [string, "null"]
          description: 빌드 시작 시각 (ISO 8601 또는 null)
        finished_at:
          type: [string, "null"]
          description: 빌드 완료 시각 (ISO 8601 또는 null)

    BuildJob:
      type: object
      required: [run_id, status, created_at, updated_at]
      description: >-
        비동기 build job 스냅샷 (#482, #480). 프로세스 메모리 상주 registry
        기반이라 서버 재시작 후 active/terminal 잡은 더 이상 조회되지 않는다.
        `response`는 성공한 잡의 최종 build 응답 본문이고, `error`는 실패한
        잡의 사유다. internal `owner_id` 필드는 wire로 노출되지 않는다.
      properties:
        run_id:
          type: string
        status:
          type: string
          enum: [queued, running, cancelling, succeeded, failed, cancelled]
          description: >-
            `POST /builds/{run_id}/cancel`(#481)이 `queued` job을 곧바로
            `cancelled`로, `running` job을 `cancelling`으로 전이시키고,
            `cancelling` job은 다음 안전한 stage 경계에서 `cancelled`가 된다.
            `succeeded`/`failed`/`cancelled`는 종단 상태이며 다시
            `running`/`cancelling`으로 돌아가지 않는다.
        created_at:
          type: string
          description: job 접수 시각 (ISO 8601)
        updated_at:
          type: string
          description: 마지막 상태 전이 시각 (ISO 8601)
        created_by:
          type: [string, "null"]
          description: 제출 principal의 display/legacy label (선택)
        response:
          type: [object, "null"]
          description: 성공한 잡의 최종 build 응답 본문 (성공 시에만)
        error:
          type: [string, "null"]
          description: 실패 사유 (실패 시에만)

    BuildsResponse:
      type: object
      required: [builds]
      properties:
        builds:
          type: array
          items:
            $ref: "#/components/schemas/BuildSummary"

    # ---------------------------------------------------------------------
    # Dataset Catalog / Stage Summary API (#488)
    # ---------------------------------------------------------------------

    DatasetSourceRef:
      type: object
      description: dataset의 canonical snapshot에 선언된 소스 참조 (secret 없음)
      required: [provider, dataset, alias]
      properties:
        provider:
          type: string
        dataset:
          type: string
        alias:
          type: string

    BuildEventName:
      type: string
      description: >-
        structured run event의 bounded vocabulary (#496). run_submitted은
        실제 async job submission이 있는 `POST /builds`(비동기)에서만 발생하며
        동기 `POST /build`에는 없다. run_cancelled(#481)도 async 전용이며 job이
        실제로 terminal `cancelled`로 확정되는 순간 run당 정확히 한 번만
        발생한다 — 같은 run에 run_finished/run_failed와 함께 나타나지 않는다.
        과도기 상태(`cancelling`)는 job status로만 노출되고 별도 event는 없다.
      enum:
        - run_submitted
        - run_started
        - run_finished
        - run_failed
        - run_cancelled
        - source_fetch_started
        - source_fetch_completed
        - source_fetch_failed
        - stage_started
        - stage_completed
        - stage_failed
        - quality_evaluated

    BuildEventStatus:
      type: string
      description: >-
        event의 결과. ok(성공/정상 완료), warn(quality_evaluated에서 FAIL 없이
        WARN이 하나 이상), fail(실패).
      enum: [ok, warn, fail]

    BuildEventStageName:
      type: string
      description: 관련 medallion stage. export는 BuildSpec.exports 실행 단계다.
      enum: [bronze, silver, gold, export]

    BuildEvent:
      type: object
      description: >-
        단일 structured run event(#496). arbitrary object dump, exception
        객체, stack trace, raw provider response, raw path, credential은
        담기지 않는다 — message/metrics는 항상 bounded하고 안전하다.
      required: [seq, timestamp, run_id, event, status, source_key, stage, message, metrics]
      properties:
        seq:
          type: integer
          description: >-
            store가 append 시점에 부여하는 monotonic ordering identifier.
            병렬 source worker의 실제 append 순서를 그대로 보존하며, 존재하지
            않는 causal ordering을 지어내지 않는다.
        timestamp:
          type: string
          format: date-time
          description: timezone-aware UTC 시각.
        run_id:
          type: string
        event:
          $ref: "#/components/schemas/BuildEventName"
        status:
          $ref: "#/components/schemas/BuildEventStatus"
        source_key:
          type: [string, "null"]
          description: 관련 소스 식별자. run 수준 event는 null.
        stage:
          oneOf:
            - $ref: "#/components/schemas/BuildEventStageName"
            - type: "null"
          description: 관련 medallion stage. source fetch/run 수준 event는 null.
        message:
          type: [string, "null"]
          description: 사람이 읽는 bounded, 안전한 요약. 없으면 null.
        metrics:
          type: [object, "null"]
          description: >-
            JSON 직렬화 가능한 안전한 수치 요약(예: records/row_count/
            check_count). 원본 provider 응답이나 임의 object는 담지 않는다.
          additionalProperties: true

    BuildEventsResponse:
      type: object
      description: run identity와 chronological ascending으로 정렬된 event 목록.
      required: [run_id, events]
      properties:
        run_id:
          type: string
        events:
          type: array
          items:
            $ref: "#/components/schemas/BuildEvent"

    StageStatusValue:
      type: string
      description: >-
        completed(완전한 산출물 존재) / failed(manifest에 실패로 기록됨) /
        not_run(상위 단계가 끝나지 못해 시도되지 않음) / unavailable(디렉터리는
        있지만 불완전·손상됨, legacy 형식 등)
      enum: [completed, failed, not_run, unavailable]

    PublishTarget:
      type: string
      description: >-
        HTTP publish API가 노출하는 target(#491). Local은 안전한 publish-root
        계약이 없고, Kaggle은 정상 pipeline packaging의 metadata.id가 publish
        destination과 정합하지 않아 이번 HTTP 계약에서 제외한다.
      enum: [huggingface]

    PublishIssue:
      type: object
      description: 안정적인 code + 사람이 읽는 message. UI가 문자열 파싱을 하지 않아도 된다.
      required: [code, message]
      additionalProperties: false
      properties:
        code:
          type: string
          description: >-
            run_not_terminal / run_failed / run_cancelled / gold_unavailable /
            artifact_missing / artifact_invalid / license_missing /
            pii_allow_with_publish / credential_unavailable 중 하나(#491).
            artifact_invalid는 canonical
            manifest.outputs 항목이 gold root를 벗어나거나(symlink escape
            포함) resolve할 수 없을 때 fail-closed로 반환된다.
        message:
          type: string

    PublishReadinessResponse:
      type: object
      description: >-
        side-effect-free readiness 판정. `ready`는 항상 `blockers`가 비어
        있는지로 계산된다. `warnings`는 게시 자체를 막지 않는다.
      required: [run_id, target, ready, blockers, warnings]
      additionalProperties: false
      properties:
        run_id:
          type: string
        target:
          $ref: "#/components/schemas/PublishTarget"
        ready:
          type: boolean
        blockers:
          type: array
          items:
            $ref: "#/components/schemas/PublishIssue"
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/PublishIssue"

    PublishRequest:
      type: object
      description: >-
        게시 요청. destination은 filesystem path로 해석되지 않고 target별
        canonical identifier("owner/name")만 허용된다. options는 target이
        실제로 지원하는 키만 허용하며(#491), 미지원 키는 조용히 무시되지 않고
        400으로 거부된다.
      required: [target, destination]
      additionalProperties: false
      properties:
        target:
          $ref: "#/components/schemas/PublishTarget"
        destination:
          type: string
          description: "target별 owner/name 형태의 게시 대상 식별자."
        options:
          $ref: "#/components/schemas/PublishHuggingFaceOptions"

    PublishHuggingFaceOptions:
      type: object
      description: >-
        신규 Hugging Face dataset repo 생성 시 visibility. 기존 repo의
        visibility는 변경하지 않는다. 생략 시 안전한 기본값 private=true다.
      additionalProperties: false
      properties:
        private:
          type: boolean
          default: true

    PublishResponse:
      type: object
      description: >-
        기존 Publisher(PublishResult, publishers/base.py)의 결과를 그대로
        노출한다. 서버 절대 경로는 담지 않는다 — reference는 안전한 외부
        참조(URL 등)만 담는다.
      required:
        - run_id
        - target
        - publisher
        - destination
        - reference
        - artifact_count
        - status
      additionalProperties: false
      properties:
        run_id:
          type: string
        target:
          $ref: "#/components/schemas/PublishTarget"
        publisher:
          type: string
        destination:
          type: string
        reference:
          type: string
          description: 게시 위치에 대한 안전한 외부 참조(URL 등). 서버 내부 경로가 아니다.
        artifact_count:
          type: integer
          minimum: 0
        status:
          type: string

    PublishBlockedResponse:
      type: object
      description: readiness 재검증에서 blocker가 발견되어 Publisher를 호출하지 않았음.
      required: [error, blockers]
      additionalProperties: false
      properties:
        error:
          type: string
        blockers:
          type: array
          items:
            $ref: "#/components/schemas/PublishIssue"

    DatasetStageMap:
      type: object
      description: source_key → {bronze, silver, gold} 상태 맵
      additionalProperties:
        type: object
        required: [bronze, silver, gold]
        properties:
          bronze:
            $ref: "#/components/schemas/StageStatusValue"
          silver:
            $ref: "#/components/schemas/StageStatusValue"
          gold:
            $ref: "#/components/schemas/StageStatusValue"

    DatasetSummary:
      type: object
      description: >-
        동일 dataset_id를 공유하는 여러 run을 접근 가능한 latest run 기준으로
        묶은 built dataset 요약. quality는 #486의 구조화된 품질 게이트가
        선반영되지 않았으므로 항상 null이다(N/A) — 미평가를 PASS로 표시하지 않는다.
      required:
        - dataset_id
        - title
        - sources
        - latest_run_id
        - status
        - updated_at
        - row_counts
        - total_row_count
        - stages
        - quality
      properties:
        dataset_id:
          type: string
        title:
          type: string
        sources:
          type: array
          items:
            $ref: "#/components/schemas/DatasetSourceRef"
        latest_run_id:
          type: string
        status:
          type: string
          enum: [ok, failed, cancelled]
        updated_at:
          type: [string, "null"]
          description: latest run의 finished_at(없으면 started_at)
        row_counts:
          type: object
          description: >-
            source_key별 Silver row_count. multi-source dataset을 단일 값으로
            축약하지 않는다.
          additionalProperties:
            type: integer
        total_row_count:
          type: integer
          description: row_counts 값의 합계
        stages:
          $ref: "#/components/schemas/DatasetStageMap"
        quality:
          type: "null"
          description: "#486 선반영 안 함 — 구조화된 quality 결과가 없으므로 항상 null"

    DatasetDetailResponse:
      type: object
      description: DatasetSummary와 동일한 필드에 run_count가 추가된 단일 dataset 상세 응답.
      required:
        - dataset_id
        - title
        - sources
        - latest_run_id
        - status
        - updated_at
        - row_counts
        - total_row_count
        - stages
        - quality
        - run_count
      properties:
        dataset_id:
          type: string
        title:
          type: string
        sources:
          type: array
          items:
            $ref: "#/components/schemas/DatasetSourceRef"
        latest_run_id:
          type: string
        status:
          type: string
          enum: [ok, failed, cancelled]
        updated_at:
          type: [string, "null"]
        row_counts:
          type: object
          additionalProperties:
            type: integer
        total_row_count:
          type: integer
        stages:
          $ref: "#/components/schemas/DatasetStageMap"
        quality:
          type: "null"
        run_count:
          type: integer
          description: 이 principal에게 접근 가능한 이 dataset_id의 run 총 개수

    DatasetsResponse:
      type: object
      required: [datasets, total]
      properties:
        datasets:
          type: array
          items:
            $ref: "#/components/schemas/DatasetSummary"
        total:
          type: integer
          minimum: 0
          description: >-
            canonical dataset grouping과 ownership/visibility 필터를 적용한 뒤,
            pagination(limit)을 적용하기 전의 접근 가능한 distinct dataset_id
            개수(#488 후속, additive). limit이 무한이면 `datasets` 길이와 같다.
            `datasets` 길이·요청한 limit·기본 page 크기를 total로 사용하지 않는다.
            다른 principal 소유 run만 있는 dataset은 포함되지 않는다.

    DatasetRunSummary:
      type: object
      required: [run_id, status, started_at, finished_at, spec_digest, created_by]
      properties:
        run_id:
          type: string
        status:
          type: string
          enum: [ok, failed, cancelled]
        started_at:
          type: [string, "null"]
        finished_at:
          type: [string, "null"]
        spec_digest:
          type: [string, "null"]
        created_by:
          type: [string, "null"]

    DatasetRunsResponse:
      type: object
      required: [dataset_id, runs]
      properties:
        dataset_id:
          type: string
        runs:
          type: array
          items:
            $ref: "#/components/schemas/DatasetRunSummary"

    StageState:
      type: object
      required: [status, available]
      properties:
        status:
          $ref: "#/components/schemas/StageStatusValue"
        available:
          type: boolean
          description: "status == completed 의 편의 boolean"

    RunStageEntry:
      type: object
      required: [source_key, bronze, silver, gold]
      properties:
        source_key:
          type: string
        bronze:
          $ref: "#/components/schemas/StageState"
        silver:
          $ref: "#/components/schemas/StageState"
        gold:
          $ref: "#/components/schemas/StageState"

    RunStagesResponse:
      type: object
      required: [run_id, sources]
      properties:
        run_id:
          type: string
        sources:
          type: array
          items:
            $ref: "#/components/schemas/RunStageEntry"

    StageDetailResponse:
      description: >-
        실제 stage 값에 따라 Bronze/Silver/Gold 상세 응답 중 하나를 반환한다.
        모든 분기는 runtime wire response의 필드를 명시하며 raw fetch_params,
        export options, credential, 절대 filesystem 경로를 노출하지 않는다.
      required: [run_id, stage, source_key, status, available]
      oneOf:
        - $ref: "#/components/schemas/BronzeStageDetailResponse"
        - $ref: "#/components/schemas/SilverStageDetailResponse"
        - $ref: "#/components/schemas/GoldStageDetailResponse"

    BronzeStageDetailResponse:
      type: object
      additionalProperties: false
      required:
        - run_id
        - stage
        - source_key
        - status
        - available
        - provider
        - dataset
        - fetched_at
        - record_count
      properties:
        run_id:
          type: string
        stage:
          type: string
          enum: [bronze]
        source_key:
          type: string
        status:
          $ref: "#/components/schemas/StageStatusValue"
        available:
          type: boolean
        provider:
          type: [string, "null"]
        dataset:
          type: [string, "null"]
        fetched_at:
          type: [string, "null"]
        record_count:
          type: [integer, "null"]

    SilverColumnInfo:
      type: object
      additionalProperties: false
      required: [name, dtype, nullable, unique_count]
      properties:
        name:
          type: string
        dtype:
          type: string
        nullable:
          type: boolean
        unique_count:
          type: integer

    SilverValidationProblem:
      type: object
      additionalProperties: false
      required: [code, field, message]
      properties:
        code:
          type: string
        field:
          type: [string, "null"]
        message:
          type: string

    SilverValidationResult:
      type: object
      additionalProperties: false
      required: [ok, problems]
      properties:
        ok:
          type: boolean
        problems:
          type: array
          items:
            $ref: "#/components/schemas/SilverValidationProblem"

    SilverStageDetailResponse:
      type: object
      additionalProperties: false
      required:
        - run_id
        - stage
        - source_key
        - status
        - available
        - row_count
        - schema
        - statistics
        - validation
        - sample
      properties:
        run_id:
          type: string
        stage:
          type: string
          enum: [silver]
        source_key:
          type: string
        status:
          $ref: "#/components/schemas/StageStatusValue"
        available:
          type: boolean
        row_count:
          type: [integer, "null"]
        schema:
          type: array
          items:
            $ref: "#/components/schemas/SilverColumnInfo"
        statistics:
          oneOf:
            - $ref: "#/components/schemas/TableStatistics"
            - type: "null"
        validation:
          oneOf:
            - $ref: "#/components/schemas/SilverValidationResult"
            - type: "null"
        sample:
          type: array
          items:
            type: object
            additionalProperties:
              $ref: "#/components/schemas/JsonValue"

    GoldExportSummary:
      type: object
      additionalProperties: false
      required: [kind]
      properties:
        kind:
          type: string

    GoldStageDetailResponse:
      type: object
      additionalProperties: false
      required:
        - run_id
        - stage
        - source_key
        - status
        - available
        - row_count
        - columns
        - splits
        - exports
        - sample
        - sample_available
      properties:
        run_id:
          type: string
        stage:
          type: string
          enum: [gold]
        source_key:
          type: string
        status:
          $ref: "#/components/schemas/StageStatusValue"
        available:
          type: boolean
        row_count:
          type: [integer, "null"]
        columns:
          type: array
          items:
            type: string
        splits:
          oneOf:
            - type: object
              additionalProperties:
                type: integer
            - type: "null"
        exports:
          type: array
          items:
            $ref: "#/components/schemas/GoldExportSummary"
        sample:
          type: "null"
        sample_available:
          type: boolean
          enum: [false]

    # ---------------------------------------------------------------------
    # Monitoring API (#516)
    # ---------------------------------------------------------------------

    MonitoringApiStatus:
      type: object
      description: >-
        Builder API 요청 처리 latency 상태. availability=available은 latency
        collector(ring buffer)가 정상적으로 표본을 읽어낼 수 있었다는 뜻이며,
        collector 자체가 손상되어 표본을 읽을 수 없으면(#527) unavailable +
        sample_count=null + p95_latency_ms=null이다 — "정상 무표본"
        (available + sample_count=0 + p95_latency_ms=null)과 "측정 불가"를
        같은 값으로 뭉개지 않는다. 이 subsystem 실패는 원 요청이나 이
        monitoring 응답 자체를 실패시키지 않는다. Healthy/Degraded 같은
        latency 임계값 판정은 근거(ADR/config)가 없어 이번 버전에서 발명하지
        않는다 — raw sample_count/p95_latency_ms만 제공한다.
      required: [availability, sample_count, p95_latency_ms]
      additionalProperties: false
      properties:
        availability:
          type: string
          enum: [available, partial, unavailable]
        sample_count:
          type: [integer, "null"]
          minimum: 0
          description: >-
            최근 최대 1000개 요청 ring buffer에 현재 보관된 표본 수.
            availability가 unavailable이면(collector 실패) null.
        p95_latency_ms:
          type: [number, "null"]
          description: >-
            sample_count가 0이거나 availability가 unavailable이면 null.
            nearest-rank 방식(보간 없음)으로 계산.

    MonitoringQueueStatus:
      type: object
      description: >-
        Async build queue 상태(#516). AsyncBuildExecutor/AsyncBuildJobRegistry
        (#511/#513)의 read-only snapshot을 반영하며, 이 서비스는 항상
        AsyncBuildExecutor를 생성하므로 정상 runtime에서는 항상
        availability: available이다. waiting/running=0은 "대기/실행 중인 job이
        실제로 없다"는 뜻이며, availability가 unavailable일 때만(예: async
        build를 지원하지 않는 구성) 나머지 필드가 null이다 — "0건"과 "확인
        불가"를 구분한다. total = waiting + running이며 terminal
        (succeeded/failed/cancelled) job 이력은 포함하지 않는다.
      required: [availability, waiting, running, total]
      additionalProperties: false
      properties:
        availability:
          type: string
          enum: [available, partial, unavailable]
        waiting:
          type: [integer, "null"]
          description: status=queued인 active job 수. unavailable이면 null.
        running:
          type: [integer, "null"]
          description: status=running인 active job 수. unavailable이면 null.
        total:
          type: [integer, "null"]
          description: waiting + running. unavailable이면 null.

    MonitoringWorkerStatus:
      type: object
      description: >-
        Async build worker pool 상태(#516). 근거는 MonitoringQueueStatus와
        동일 — AsyncBuildExecutor가 항상 생성되므로 정상 runtime에서는 항상
        availability: available이다. utilization은 0.0~1.0 ratio다(퍼센트
        아님). unavailable일 때만 나머지 필드가 null이다.
      required: [availability, active, capacity, utilization]
      additionalProperties: false
      properties:
        availability:
          type: string
          enum: [available, partial, unavailable]
        active:
          type: [integer, "null"]
          description: 현재 running job 수. unavailable이면 null.
        capacity:
          type: [integer, "null"]
          description: >-
            AsyncBuildExecutor 생성 시 지정한 max_workers. unavailable이면
            null.
        utilization:
          type: [number, "null"]
          description: >-
            active / capacity ratio, 0.0~1.0. capacity가 0보다 클 때만 계산되며
            unavailable이면 null(0.0으로 위장하지 않는다).

    MonitoringArtifactStatus:
      type: object
      description: >-
        Artifact Store 상태. output_root 폴더가 존재한다는 사실만으로
        available로 간주하지 않는다 — BuildIndex 쿼리도 성공해야 available이다.
        절대 파일시스템 경로는 노출하지 않는다.
      required: [availability, last_write_at]
      additionalProperties: false
      properties:
        availability:
          type: string
          enum: [available, partial, unavailable]
        last_write_at:
          type: [string, "null"]
          format: date-time
          description: >-
            BuildIndex에 기록된 가장 최근 성공(ok) 빌드의 완료 시각. 성공 기록이
            없으면 available이되 null(0건과 확인 불가를 구분).

    MonitoringSummaryResponse:
      type: object
      description: >-
        Studio Monitoring 화면용 시스템 상태 요약 (#516). Provider 상태는
        요청마다 네트워크 프로브를 유발하므로(#492) 이번 버전에서는 포함하지
        않는다.
      required: [generated_at, status, api, queue, workers, artifact_store]
      additionalProperties: false
      properties:
        generated_at:
          type: string
          format: date-time
        status:
          type: string
          enum: [healthy, degraded]
          description: >-
            required subsystem(api/queue/workers/artifact_store) availability로부터
            계산되는 deterministic aggregate status. 넷 모두 available이면
            healthy, 하나라도 partial/unavailable이면 degraded. latency
            threshold(SLA)는 사용하지 않으며 sample_count=0/p95_latency_ms=null
            자체는 degraded 근거가 아니다. Provider status(#492, optional)는
            이 판정에 포함되지 않는다.
        api:
          $ref: "#/components/schemas/MonitoringApiStatus"
        queue:
          $ref: "#/components/schemas/MonitoringQueueStatus"
        workers:
          $ref: "#/components/schemas/MonitoringWorkerStatus"
        artifact_store:
          $ref: "#/components/schemas/MonitoringArtifactStatus"

    MonitoringBuildBucket:
      type: object
      description: >-
        [bucket_start, bucket_end) 반열린 구간의 build 카운트 (UTC). success는
        내부 BuildIndex status "ok"를 외부 wire 명명으로 매핑한 값이다(#527) —
        내부 BuildIndex status vocabulary(ok/failed/cancelled) 자체는 바뀌지
        않는다.
      required: [bucket_start, bucket_end, total, success, failed, cancelled]
      additionalProperties: false
      properties:
        bucket_start:
          type: string
          format: date-time
        bucket_end:
          type: string
          format: date-time
        total:
          type: integer
          minimum: 0
        success:
          type: integer
          minimum: 0
        failed:
          type: integer
          minimum: 0
        cancelled:
          type: integer
          minimum: 0

    MonitoringRecentRun:
      type: object
      description: Monitoring 화면용 최소 run 요약. dataset_id/owner_id/error 등은 노출하지 않는다.
      required: [run_id, status, started_at, finished_at]
      additionalProperties: false
      properties:
        run_id:
          type: string
        status:
          type: string
          enum: [ok, failed, cancelled]
        started_at:
          type: [string, "null"]
        finished_at:
          type: [string, "null"]

    MonitoringBuildsResponse:
      type: object
      description: >-
        window/bucket별 build 통계(#516). timezone은 UTC. bucket 기준
        timestamp는 finished_at. excluded_count는 malformed timestamp로 집계에서
        제외된 행 수이며, 0보다 크면 availability는 partial이다.
      required: [window, bucket, availability, excluded_count, buckets, recent_runs]
      additionalProperties: false
      properties:
        window:
          type: string
          enum: [24h]
        bucket:
          type: string
          enum: [hour]
        availability:
          type: string
          enum: [available, partial, unavailable]
          description: >-
            available: 집계 성공(0건이어도 유효). partial: BuildIndex 쿼리는
            성공했지만 malformed timestamp로 일부 행이 제외됨. unavailable:
            BuildIndex 쿼리 자체가 실패함(buckets는 빈 배열).
        excluded_count:
          type: integer
          minimum: 0
        buckets:
          type: array
          items:
            $ref: "#/components/schemas/MonitoringBuildBucket"
        recent_runs:
          type: array
          items:
            $ref: "#/components/schemas/MonitoringRecentRun"
